interface:
  display_name: "SmartBot"
  short_description: "Repo-specific patterns and workflows for SmartBot"
  default_prompt: "Use the SmartBot repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true