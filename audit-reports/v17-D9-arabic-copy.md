# v17-D9 — تدقيق جودة النصوص العربية للواجهة (UX Writing)

> **Task ID:** v17-D9 · **النوع:** وكيل تشخيص (READ-ONLY — لا تعديل كود)
> **النطاق:** الدقة اللغوية والاتساق عبر كل `src/app` و`src/components` (189 ملف .ts/.tsx) — المصطلحات، الجمع مع الأعداد، صياغة الأزرار، نبرة الـtoast، الأرقام، الترقيم، الـplaceholders، رسائل التحقق، البقايا الإنجليزية، الإملاء.
> **العقد المفحوص:** `scripts/check_i18n_calls.py` → **PASS** (0 نداء toLocale خارج `lib/format.ts`) · `lib/arabic-plural.ts` (محرك الأعداد الستة) · `lib/format.ts` (الدرزة الوحيدة: أرقام لاتينية 0-9 + ar-LY + أسماء أشهر عربية + ساعة 24).

---

## 1) الملخص التنفيذي

| المقياس | القيمة |
|---|---|
| ملفات مفحوصة (`rg` منهجي + قراءة) | 189 ملف (src/app + src/components + lib) |
| بوابات i18n | ✅ PASS — الدرزة format.ts مُفعَّلة فعليًا (لا أحد يستدعي toLocaleString خارجها) |
| استدعاءات `countPhrase` (جمع عربي سليم) | **32 استدعاءً في 13 ملفًا** — النظام مستخدم لا هامش |
| محاربات جمع خام `${n} noun` متبقية في كود حي | **1 فقط** (dashboard/page.tsx:174) — كانت ~15 قبل v8 |
| مفاهيم برسمين مختلفين (نفس الشيء، كلمتان) | **4**: خطة/باقة · fan_count بأربعة مسميات · إشعار/تنبيه · فشل/تعذر للتحميل |
| مسارات تسريب رسائل خطأ إنجليزية خام إلى المستخدم | **2 مسار** (شبكة/فيسبوك) + بوابة ApiError سليمة |
| بقايا إنجليزية في نصوص UI الظاهرة | شبه صفر (شارات `INFO/WARN` في صفحة العرض التجريبي فقط) |
| أخطاء إملاء/اتفاق صريحة | 2 («موافقون مضافة» · «آخر 30 يوم» بلا تنوين) + تنويعة ألف التوكيد مختلفة في ملفات الاختبار |

**الخلاصة:** الطبقة «الهيكليّة» للعربية ممتازة (محرك جمع، درزة تنسيق، بوابة CI) — الفجوة كلها في **الاتساق التحريري**: مفهوم الشراء اسمه «خطة» في 4 تدفقات و«باقة» في 3 أسطح، ونفس مقياس fan_count له 4 مسميات، ونبرة toast الناجح يتذبذب بين «تم X» و«تم X بنجاح» و«تم X بنجاح!» و«✅ تم X».

---

## 2) اتساق المصطلحات (§1 من النطاق)

### 2-أ) «خطة» مقابل «باقة» — المفهوم نفسه، كلمتان، حتى داخل الملف الواحد

| الموضع | الكلمة المستخدمة |
|---|---|
| `components/shared/payment/index.tsx:77` + `:470` + `:515` | «**تفعيل الخطة** المجانية» |
| `components/shared/payment/index.tsx:254` (نفس الملف) | «سعر **الباقة** غير صالح — أعد فتح نافذة الدفع» |
| `components/shared/payment/index.tsx:76` (تعليق العقد) | «المبلغ: 0 د.ل — **الباقة**: مجاني» |
| `app/pricing/page.tsx:83` «خطط الأسعار» · `:94` «خطط تناسب كل الأحجام» · `:156` «جارٍ تحميل **الخطط**» · `:167` · `:177` | **الخطط** |
| `app/pricing/layout.tsx:6` «الأسعار و**الباقات**» · `:8` «**باقات** SmartBot المرنة…» · `:11` «**باقات** وأسعار SmartBot» | **الباقات** — نفس مسار /pricing! |
| `app/onboarding/OnboardingWizard.tsx:86` «جميع **الباقات** تبدأ بتجربة مجانية» · `:551` زر «عرض كل **الباقات**» | **الباقات** — ثم الزر يقود إلى /subscribe حيث كل شيء «خطة» |
| `app/subscribe/PlanSelector.tsx:49` «اختر **خطة**» · `:123` «متابعة مع **خطة** X» · `StepIndicator.tsx:18` · `PaymentSection.tsx:56` | **الخطة** |
| `components/landing/sections/FinalCTASection.tsx:53` «عرض **الخطط**» · `landing-data.ts:27,29` «**الخطة** المجانية» · `Header.tsx:14` «**الخطط** والأسعار» · `Footer.tsx:13` «**الخطط**» | **الخطط** |
| `app/admin/page.tsx:257` عمود الجدول «ال**خطة**» | **الخطة** |

**الحصيلة:** خطة/خطط ≈ 44 موضعًا مقابل باقة/باقات ≈ 9 مواضع — لكن مواضع «الباقة» تجلس في **قلب مسار المال** (نافذة الدفع + عنوان SEO لصفحة الأسعار + معالج التأهيل). المستخدم يقرأ «عرض كل الباقات» في المعالج فيهبط في صفحة «اختر الخطة» ويضغط «ادفع الآن» فتفتح نافذة عنوانها «دفع الاشتراك» وخطأها «سعر الباقة غير صالح».

### 2-ب) fan_count (متابعو صفحة فيسبوك) — مقياس واحد بأربعة مسميات

| الموضع | التسمية |
|---|---|
| `app/dashboard/page.tsx:185` | «متابعو الصفحة» (KPI) |
| `app/dashboard/audience/page.tsx:62` | «**إجمالي المعجبين**» |
| `app/dashboard/audience/page.tsx:42` | العنوان الفرعي «تحليل الجمهور و**المتابعين**» — في نفس الصفحة التي تعدّهم «مشتركون» (`:122` التبويب، `:146` الحالة الفارغة) |
| `app/dashboard/analytics/page.tsx:31` | «**المتابعين**» |
| `app/demo/page.tsx:193` | «**المتابعون**» |
| `components/onboarding/OnboardingTour.tsx:39` | «قائمة **المشتركين والمتابعين**» (يشير لعنصر القائمة الجانبية المسماة «الجمهور») |

مفهوم «أشخاص تفاعلوا مع الصفحة» يدور بين: الجمهور / المشتركون / المتابعون / المعجبين. التوزيع الدلالي (مشترك = اشترك بالماسنجر، متابع = fan_count) **مقصود وله منطق** — لكن صفحة الجمهور تخلطه على الشاشة الواحدة: العنوان «المتابعين» والمحتوى «المشتركون»، وتوقيع المحادثة `audience/page.tsx:125` «يتغذّى تلقائياً من محادثات الماسنجر».

### 2-ج) «إشعار» مقابل «تنبيه» — داخل صفحة واحدة

| الموضع | الكلمة |
|---|---|
| `app/dashboard/notifications/page.tsx:187` | عنوان الصفحة «**الإشعارات**» (والقائمة الجانبية `MobileBottomNav.tsx:27` «الإشعارات») |
| `app/dashboard/notifications/page.tsx:78` | تصنيف «تنبيهات الدفع» (payment_alerts) |
| `app/dashboard/notifications/page.tsx:301` | «إعدادات **التنبيهات**» — قسم الإعدادات داخل نفس الصفحة |
| `app/dashboard/notifications/page.tsx:124,136` | toast: «فشل تحديد **الإشعارات** كمقروءة» · «فشل تحديد **الإشعار** كمقروء» |

صفحة واحدة تسمّي عناصرها «إشعارات» في الواجهة و«تنبيهات» في الإعدادات والتصنيف.

### 2-د) «رسالة» مقابل «محادثة» — ✅ سليم (نتيجة إيجابية)

التمييز مطبَّق بدقة دلالية صحيحة: محادثة = الخيط (`messages/page.tsx:253` «لا توجد محادثات بعد»، `:276` «اختر محادثة»، `dashboard/page.tsx:173` «محادثات الماسنجر») ورسالة = الوحدة (`messages/page.tsx:300` «لا توجد **رسائل** في هذه المحادثة»، `:256` «فور وصول أول **رسالة**»). لا تناقض.

### 2-هـ) «فشل تحميل» مقابل «تعذر تحميل» — الفعل نفسه بصيغتين

| «فشل تحميل X» | «تعذر تحميل X» |
|---|---|
| `app/subscribe/SubscribeContent.tsx:59` و`:92` (toast) | `app/subscribe/SubscribeContent.tsx:154` — **نفس الملف، نفس الفشل**: toast «فشل تحميل الخطط» والحالة الفارغة «تعذر تحميل الخطط» |
| `app/dashboard/leads/page.tsx:18,48` · `autoreply:28,156` · `admin/telegram:102,116,130,250` · `admin/support:174` · `admin/page.tsx:232` | `app/dashboard/audience:96,141` · `messages:244` · `team:52` · `pricing/page.tsx:167` · `global-error.tsx:43` |

### 2-و) النقَل الحرفي — ✅ سليم

تليجرام (21) · ماسنجر (17) · ويبهوك (12) · فيسبوك (61) — لا تباين في الكتابة إطلاقًا. والدعم يُسمى «الدعم» في كل المواضع (لا «المساعدة»).

---

## 3) الجمع العربي مع الأعداد (§2)

**الحالة العامة ممتازة:** `countPhrase` مستدعى في 13 ملفًا (connect، analytics، audience، autoreply، marketing، messages، notifications، dashboard، pages، reports، demo، OnboardingWizard) ويعطي «لا رسائل / رسالة / رسالتان / 5 رسائل / 15 رسالة» وفق محرك arabic-plural.ts. المراجعون تركوا تعليقات تتبع (`marketing/page.tsx:113` «v12-E5.5: was raw `${n} مشترك`»).

| الموضع | المشكلة | الصياغة المقترحة |
|---|---|---|
| `app/dashboard/page.tsx:174` | `` `${toArabicNumber(messages.unread_conversations \|\| 0)} غير مقروءة` `` — عدّ + صفة **بلا اسم**: عند 1 → «1 غير مقروءة» (منكرة نحويًا: الواحد لا يُعدّ بهذه الصيغة)، وعند 3 → «3 غير مقروءة» (محذوف المعدود). لا يمر عبر countPhrase | `countPhrase(n, "محادثة غير مقروءة", "محادثتان غير مقروءتان", "محادثات غير مقروءة")` |
| `app/dashboard/audience/page.tsx:122` | `المشتركون (${subsQuery.data?.total ?? 0})` — العدّ بين قوسين وليس عبارة عربية (مقبول كأسلوب لكنه خارج نظام countPhrase المستخدم في الصفحة نفسها `:104`) | «المشتركون · 5» أو countPhrase |
| `app/dashboard/analytics/page.tsx:77` | «الردود اليومية (آخر 30 **يوم**)» — بلا تنوين نصب (المعدود 3–10 يوجب «30 يومًا») — وبعد 8 أسطر `:85` يكتبها صحيحة «خلال آخر 30 **يوماً**» — عدم اتساق داخل المكوّن | «آخر 30 يوماً» في الموضعين |
| `components/subscribe/plan-comparison.test.ts:78-79` | الاختبار يثبّت «حتى 5 ردود»/«حتى 500 رد» — يمر عبر repliesPhrase (سليم) — مذكور للأمانة فقط | — |

لا توجد أي قوالب «N رسالة/N مشترك/N قاعدة» خام في JSX حي؛ ما تبقى تعليقات كود فقط (`marketing:113`).

---

## 4) صياغة الأزرار: فعل أمر أم مصدر؟ (§3)

| النمط | المواضع |
|---|---|
| **مصدر (مهيمن في نماذج لوحة التحكم)** | «جدولة» `scheduled/page.tsx:121` · «نشر» `posts/page.tsx:97` · «حفظ الحملة» `marketing/page.tsx:223` · «حفظ القاعدة» `autoreply/page.tsx:142` · «حفظ» `tools/page.tsx:101` · «إنشاء البث» `broadcast/page.tsx:129` · «إرسال الطلب» `support/page.tsx:348` · «تغيير كلمة المرور» `settings/page.tsx:171` · «حفظ وتفعيل» `connect/page.tsx:379` · «حفظ الإعدادات» `admin/telegram/TelegramConfigSection.tsx:102` · «إضافة» `DiagnosticsSection.tsx:102` · «تسجيل الدخول» `login/page.tsx` · «إنشاء حساب» `register/RegisterForm.tsx:256` · «دفع الاشتراك» / «تفعيل الخطة المجانية» `payment/index.tsx:470` |
| **فعل أمر (مهيمن في مسار الشراء والـCTA التسويقي)** | «اشترك الآن» / «ابدأ مجاناً» `pricing/page.tsx:278` · «ادفع الآن (X د.ل)» `subscribe/PaymentSection.tsx:60` · «ادفع عبر المحفظة الإلكترونية» `payment/index.tsx:474` · «أرسل المبلغ إلى ليبيانا» `payment-instructions.tsx:53` · «اختر خطة أولاً» / «متابعة مع خطة X» `PlanSelector.tsx:123` · «ابدأ الآن» `OnboardingWizard.tsx:624` · «أنشئ قاعدة» `SetupWarnings.tsx:99` (CTA تحذير) |
| **اصطدام مباشر في التدفق الواحد** | الزر المفتوح يقول «**ادفع الآن**» (`PaymentSection.tsx:60`) فيفتح نافذة زر إرسالها «**دفع الاشتراك**» (`payment/index.tsx:470`) — أمر ثم مصدر على شاشتين متتاليتين لنفس الفعل |

**التقييم:** المصدر مقصود في النماذج الداخلية (تقليد UX عربي شائع ومقبول) والأمر مقصود في البيع — لكن عدم ترسيم القاعدة أدى لتباين على حدود المسارين. «إلغاء» موحّد (`autoreply:140`، `tools:100`، `marketing:224`، `broadcast:87`). أزرار المعالج/الجولة متطابقة («السابق/التالي/تخطي/إنهاء» — `OnboardingWizard.tsx:595,624` + `OnboardingTour.tsx:129-133`). حالات الانتظار متسقة تمامًا: «جارٍ الحفظ…/جارٍ الإنشاء…/جارٍ الإرسال…/جارٍ التغيير…/جارٍ التحميل…».

---

## 5) رسائل toast والنبرة (§4)

### 5-أ) النجاح: «تم X» هو المعيار (≈35 موضعًا) — والانحرافات:

| الانحراف | المواضع |
|---|---|
| إضافة «بنجاح» (8 مواضع من ~43) | `login/page.tsx:147` · `register:106` · `admin/telegram/page.tsx:169` · `settings:51` · `support:155` · `subscribe/SubscribeContent.tsx:111` · `payment/index.tsx:90` · `support:359` — بينما «تم إنشاء القاعدة/الحملة/البث/المنشور…» كلها بلا «بنجاح» |
| علامة تعجب «!» | `admin/telegram/page.tsx:169` «…بنجاح!» · `support/page.tsx:359` «…بنجاح!» · `SubscribeContent.tsx:111` «…بنجاح! جارٍ نقلك…» |
| إيموجي داخل نص الـtoast | `connect/page.tsx:104` «✅ الاتصال ناجح!» · `:128` «✅ تم حفظ البيانات…» · `OnboardingWizard.tsx:444` «✗ »+خطأ — كل toasts الأخرى بلا رموز (brandedToast يرسم أيقونته الخاصة) — تكرار بصري |
| فاصل نقطي أوسط | `dashboard/pages/page.tsx:65` «اتصال ناجح · 5 متابعين» — «·» لا تُستخدم في أي toast آخر |
| تأنيث الفاعل الصحيح «تمت» | `DiagnosticsSection.tsx:55` «تمت إضافة الموافق» · `admin/telegram/page.tsx:195` «تمت إضافة جهة الإرسال» — صحيح نحويًا (تمت + إضافة) ✅ ولا يوجد «تم إضافة» خاطئ |

**لا يوجد** «نجح» وحده (لا موضع واحد) — الاتساق الأساسي قوي.

### 5-ب) الخطأ: «فشل X» هو المعيار ✅ — باستثناء خلط «فشل/تعذر» للتحميل (§2-هـ أعلاه).

### 5-ج) تسريب أخطاء إنجليزية خام إلى المستخدم — مساران حيّان:

1. **رفض fetch الشبكي (انقطاع الإنترنت/DNS):** `lib/csrf-client.ts:132` — `await fetch(url, …)` بلا try/catch؛ عند الرفض يرمي المتصفح TypeError برسالة إنجليزية («Failed to fetch») تصل كما هي إلى `e.message` ثم تُعرض مباشرة في ~20 معالج `onError: (e) => brandedToast.error(e.message)` أو `e.message || "فشل…"` — عامل `||` لا ينقذ لأن الرسالة الإنجليزية truthy. أمثلة: `autoreply/page.tsx:57,63` · `tools:54,60,66` · `scheduled:69,79` · `team/page.tsx:52` · `admin/telegram/page.tsx:159,170`. (بوابة ApiError نفسها سليمة: `csrf-client.ts:7-10` تعرض detail الباك-إند العربي وتحتياطها «فشل الطلب (500)» عربي، والباك-إند `routers/*.py` يرسل detail عربيًا في المواضع المفحوصة: `facebook_routes.py:329` «فشل النشر»، `ai.py:76`، `admin_routes.py:345`، `dashboard_stats.py:277`.)
2. **مغلفات الباك-إند تمرر str(e) الإنجليزية:** `fb_dashboard/routers/facebook_routes.py:274` — `"error": str(e)[:200]` و`:202` `webhook_result = {"error": str(e)[:200]}` و`routers/onboarding.py:225` — `"error": f"تعذر الاتصال بفيسبوك: {str(e)[:150]}"` — رسائل Graph API الإنجليزية تصل نصًا إلى `connect/page.tsx:107-108` (`td.error`) وتُعرض في errorMsg وtoast.

**حارس موجود:** `components/shared/DefaultError.tsx:36` (v9-E9) يمنع error.message الإنجليزية في حدود الأخطاء — لكنه لا يغطي toasts الـmutation ولا مسار المغلفات.

---

## 6) الأرقام: لاتينية أم هندية-عربية؟ (§5) — ✅ سليم

- البوابة تمر: 0 نداء `toLocale*` خارج `lib/format.ts` في 189 ملفًا.
- الدرزة (`format.ts:5-9`): «ar-LY» — أرقام لاتينية 0-9 + فواصل الآلاف النقطية «1.234.567». `toArabicNumber` تضمن عدم ظهور ٠-٩ أبدًا.
- **لا توجد أرقام هندية-عربية في أي نص ظاهر للمستخدم.** الوحيدة: تعليقا كود فقط — `OnboardingWizard.tsx:410` («الخطة ٥.١») و`PlanSelector.tsx:105` («"٣+"») — لا يراهما المستخدم.
- العملة متسقة: «د.ل» و«د.ل/شهر» (`PaymentSection.tsx:39`، `OnboardingWizard.tsx:540`، `payment/index.tsx:502`).
- `demo/page.tsx:110` «12.500» ثابت يدوي يطابق اصطلاح ar-LY.

---

## 7) الترقيم (§6) — ✅ نظيف تقريبًا

- **علامة الاستفهام:** 31 «؟» عربية، **صفر** «?» لاتينية ملاصقة لعربية.
- **الفواصل:** لا «,» لاتينية داخل جمل عربية في كود حي (الموضع الوحيد `plan-comparison.test.ts:76` وصف اختبار إنجليزي).
- علامة الشرطة «—» متسقة عبر التطبيق. الحذف «…» متسق؛ «...» الثلاثية تظهر فقط في placeholders تقنية LTR (`EAAx...`، `sk-proj-...`) — مقبول.
- **التنويعة الوحيدة:** أسلوب اقتباس مزدوج — «…اختر "تحويل بنكي"…» (`payment-methods.tsx:78` بعلامات &quot; مستقيمة) مقابل «…اقترح رداً"…» (`OnboardingWizard.tsx:514`) مقابل «» الفرنسية في تعليقات الكود فقط. توحيد المقترح: «» الفرنسية في النص الظاهر.

---

## 8) placeholders الحقول (§7)

| النمط | المواضع | التقييم |
|---|---|---|
| **مثال قيَمي «مثال: X»** | `OnboardingWizard.tsx:390,473,504` · `DiagnosticsSection.tsx:96` · `BroadcastTargetsSection.tsx:58` · `autoreply:97,111` · `broadcast:107` | ✅ أفضل ممارسة |
| **إرشادية أمرية** | `pages/page.tsx:166` «أدخل معرف الصفحة من فيسبوك» · `:170` «أدخل رمز الوصول» | مقبولة لكنها النمط الوحيد من نوعه (بقية الصفحات «مثال:») |
| **حثّ على الكتابة «اكتب X…»** | `marketing:176` · `messages:385` · `comments:135` · `posts:85` · `support:327` · `demo:343` | ✅ متسقة للمربعات النصية |
| **مكررة من الـlabel حرفيًا** | `register/RegisterForm.tsx:167` («اسم المستخدم» = label `:165`) · `:183` · `:200` · `:223` · `login/page.tsx:215,227` | ⚠️ تكرار بلا قيمة: عند الإدخال يختفي الـplaceholder فيبقى الحقل بلا تذكير — لا أمثلة ولا تنسيق متوقع. الأفضل: «مثال: ahmed@example.com» للبريد |
| **قيم تقنية LTR** | `connect:304,322` · `payment-instructions:104` «09XXXXXXXX» · `TelegramConfigSection:87,96` | ✅ مع dir="ltr" صحيح |

لا يوجد placeholder فارغ ولا إنجليزي (غير التقني) في أي صفحة عربية.

---

## 9) رسائل التحقق validation (§8) — ✅ أقوى نقطة

| الموضع | الرسالة | التقييم |
|---|---|---|
| `register/RegisterForm.tsx:49` | «يرجى إدخال اسم المستخدم» | ✅ |
| `:50` | «اسم المستخدم يجب أن يكون 3 أحرف على الأقل» | ✅ |
| `:52` | «أدخل بريداً إلكترونياً صالحاً» | ✅ (الوحيدة بصيغة أمر بين «يرجى» — تباين طفيف) |
| `:56,58` | «كلمة المرور يجب أن تكون 8 أحرف على الأقل» · «كلمتا المرور غير متطابقتين» | ✅ صحيحة نحويًا |
| `payment/index.tsx:250` | «رقم الهاتف يجب أن يبدأ بـ 09 ويتكون من 10 أرقام (مثال: 0912345678)» | ✅ نموذجية: قاعدة + مثال |
| `support/page.tsx:338` | «الرسالة يجب أن تكون 10 أحرف على الأقل» (role="alert") | ✅ |
| `settings/page.tsx:35` | «كلمة المرور الجديدة يجب أن تكون 8 أحرف على الأقل» | ✅ |
| `admin/telegram/page.tsx:142` · `DiagnosticsSection.tsx:42` | «يرجى إدخال معرف المحادثة/تليجرام» | ✅ |

النظام متماسك: «يرجى إدخال» للفراغ + «يجب أن» للقيد. لا رسائل إنجليزية ولا غائبة في النماذج المفحوصة.

---

## 10) البقايا الإنجليزية في صفحات عربية (§9) — شبه نظيف

| الموضع | البقايا | الخطورة |
|---|---|---|
| `app/demo/page.tsx:108-111,407` | شارات مستوى السجل تُرسم حرفيًا: «INFO»/«WARN» في صفحة عرض عربية بالكامل | منخفضة (صفحة عرض؛ لكنها الوحيدة) |
| `app/dashboard/audience/page.tsx:103` | «معلق #12345» — «#» قبل الرقم في سياق RTL (يظهر يسار الرقم عند bidi الخاطئ) | شكلية |
| مصطلحات تقنية مقصودة | «Chat ID»/«User ID»/«Post Messages»/«BotFather»/«/start» (`TelegramConfigSection:84,88`، `admin/telegram:281,292`) | ✅ مقبولة — أسماء علم تقنية |
| ملفات الاختبار | نصوص إنجليزية وصفية | لا تظهر للمستخدم |

عناوين كل الصفحات، القوائم الجانبية، حدود الأخطاء (`global-error.tsx:43`، `DefaultError.tsx`، `[...slug]/page.tsx:27-35`)، الشروط والخصوصية — عربية خالصة.

---

## 11) الإملاء (§10)

| الموضع | الخطأ | الصواب |
|---|---|---|
| `admin/telegram/DiagnosticsSection.tsx:108` | «لا يوجد موافقون **مضافة**» | «لا يوجد موافقون **مضافون**» — الصفة مؤنث لا تحيل لأي مرجع (موافقون مذكر) |
| `analytics/page.tsx:77` | «آخر 30 **يوم**» بلا تنوين (والسطر `:85` الصحيح «30 يوماً») | «آخر 30 يومًا» — انظر §3 |
| أسلوب ألف التوكيد | الكود الحي يكتب «يوماً/فوراً/أيضاً/شهرياً/مجاناً» (تنوين على الحرف ثم ألف — 106 موضعًا) بينما 4 ملفات اختبار تكتب «يومًا/لاحقًا» (تنوين فوق الألف): `PageHeader.test.tsx:113,118` · `ChartCard.test.tsx:20,26` · `EmptyState.test.tsx:28,31` | توحيد العائلة (يفضَّل «Xاً» لأنها الغالبة في الحي) — لا يظهر للمستخدم الآن لكن الاختبارات تثبّت الرسم الخطأ |
| تاء مربوطة/هاء · ي/ى · همزات | **صفر مخالفات** بعد مسح «انشاء/ارسال/اذا/الرساله/فى/الى…» | ✅ |
| «جارٍ» (بالكسرتين) | متسقة في كل المواضع ✅ | — |

---

## 12) أخطر 10 (مرتبة بالأثر على مسار المال والثقة)

| # | المواضع | المشكلة | الإصلاح المقترح (لجولة E) |
|---|---|---|---|
| 1 | `payment/index.tsx:77↔254` · `pricing/layout.tsx:6↔pricing/page.tsx:83` · `OnboardingWizard:86,551` | «خطة»/«باقة» لنفس المفهوم — داخل نافذة الدفع نفسها وفي مسار /pricing نفسه | قرار معجم واحد («خطة» — الغالبة 44:9) + استبدال 9 مواضع «باقة» + فحص CI اختياري `rg -c "باقة" src` = 0 |
| 2 | `csrf-client.ts:132` + ~20 معالج `e.message` | انقطاع الشبكة يعرض toast إنجليزية «Failed to fetch» للمستخدم الليبي | لفّ fetch بـ try/catch يرمي `ApiError(0, {detail: "تعذر الوصول إلى الخادم — تحقق من اتصالك"})` |
| 3 | `routers/facebook_routes.py:274,202` · `onboarding.py:225` | str(e) لرسائل Graph API الإنجليزية تصل المستخدم عبر `connect:107` | تحويل إلى detail عربي ثابت + الكود التقني إلى السجلات |
| 4 | `audience/page.tsx:42,62,122` · `dashboard:185` · `analytics:31` | مقياس fan_count بأربعة أسماء + صفحة الجمهور تخلط «متابعين/مشتركون» على شاشة واحدة | جدول مسميات: fan_count=«متابعي الصفحة»، مشتركو الماسنجر=«المشتركون»؛ توحيد «إجمالي المعجبين» |
| 5 | `notifications/page.tsx:78,301` مقابل `:187,124` | «إشعار/تنبيه» في صفحة الإشعارات نفسها | اعتماد «إشعار» + «إعدادات الإشعارات»/«إشعارات الدفع» |
| 6 | `dashboard/page.tsx:174` | «N غير مقروءة» — العد الوحيد خارج نظام الجمع؛ «1 غير مقروءة» منكرة | `countPhrase(n, "محادثة غير مقروءة", …)` |
| 7 | `PaymentSection.tsx:60` ↔ `payment/index.tsx:470` | «ادفع الآن» يفتح نافذة زرها «دفع الاشتراك» — أمر/مصدر على شاشتين متتاليتين | قاعدة: أزرار الشراء أمرًا («ادفع الآن») أو المصدر في كلتيهما |
| 8 | 8 مواضع «بنجاح» + «!» + «✅» (§5-أ) | نبرة النجاح غير موحدة | معيار واحد: «تم X» + يمنع «✅/✗/!» من نصوص toast (brandedToast يرسم الأيقونة) |
| 9 | `SubscribeContent.tsx:59↔154` + خريطة §2-هـ | «فشل تحميل/تعذر تحميل» للفعل نفسه | «تعذر تحميل X» للقراءة و«فشل X» للكتابة — قاعدة قابلة للـgrep |
| 10 | `DiagnosticsSection.tsx:108` · `analytics:77` · placeholders السجل/الدخول (§8، §11) | «موافقون مضافة» + «30 يوم» بلا تنوين + placeholder=label في أربعة حقول دخول/تسجيل | إصلاحات سطرية + أمثلة في placeholders («مثال: 0912345678») |

---

## 13) ما هو سليم بالفعل (يُبنى عليه ولا يُمس)

1. **بنية الجمع**: arabic-plural + countPhrase + الاختبارات (`arabic-plural.test.ts`, `format.edge.test.ts`) — من أفضل ما يمكن أن يكون في مشاريع عربية.
2. **درزة الأرقام**: بوابة CI حقيقية (189 ملفًا، صفر مخالفة) + تعليق تاريخي يوثق الفوضى السابقة.
3. **الترقيم العربي**: ؟ كاملة، لا فواصل لاتينية في الجمل العربية.
4. **رسائل التحقق**: عربية + قاعدة+مثال + ربط aria صحيح.
5. **النقل الحرفي** (تليجرام/ماسنجر/ويبهوك/فيسبوك) والعملة (د.ل) و«جارٍ» وحالات الانتظار — متطابقة عبر التطبيق.
6. **DefaultError (v9-E9)** يمنع error.message الإنجليزية في حدود الأخطاء.
7. **تم/تمت** التأنيث الصحيح في كل toasts الإضافة.

## 14) خطوات تالية مقترحة (لجولة الإصلاح)

1. معجم مصطلحات واحد (`docs/arabic-glossary.md`): خطة/مشترك/متابع/عميل/محادثة/إشعار + قرارات فشل/تعذر + صيغة الأزرار — ثم تنفيذ الاستبدالات (كلها نصية، ~35 سطرًا).
2. لفّ `fetch` الشبكي في `apiFetch` برسالة عربية (سطر واحد + اختبار).
3. مسح الظهر: `rg -n "باقة|تنبيه|المعجبين|بنجاح|✅" src/app src/components` كقائمة مراجعة يدوية، أو بند جديد في `check_i18n_calls.py` (اختياري).
4. رفع إصلاح str(e) في `facebook_routes.py`/`onboarding.py` لفريق الباك-إند (خارج نطاق الواجهة لكنه المصدر).
