# تقرير الجولة v10 — الأضخم والأعمق: gstack مسلّحة بكل قواها + 16 وكيلًا

**التاريخ:** 2026-09-06 · **الأساس:** main @ 38537f9e → **النتيجة:** v10 (commit هذا التقرير) · **الطلب:** «فحص وتشخيص عميق جدًا ومفصل بمساعدة gstack… استخدمها بكل قواها… الجولة الأقوى والأضخم والأعمق والأدق لكل الجوانب»

## 1) ما حدث — البنية غير المسبوقة

ثُبّتت **gstack** (github.com/garrytan/gstack) كاملةً (55 مهارة، بناء + تسجيل رسمي عبر ./setup) ثم استُخدمت **منهجياتها الفعلية** لا مجرد إلهام: كل وكيل تشخيص قرأ ملف SKILL.md الخاص بمهارته واتبع خطواته حرفيًا.

**موجة التشخيص — 11 وكيلًا متوازيًا:**

| الوكيل | منهجية gstack | الحصيلة |
|---|---|---|
| G1 | /cso --comprehensive (14 مرحلة) | 17 نتيجة أمنية: CRIT + 3 HIGH + 7 MED (منها حفر تاريخ git — **لأول مرة**) |
| G2 | /qa-only | 9 أعطال عبر 116 لقطة + 44/44 تحميل لوحة — صحة 88/100 |
| G3 | /benchmark | مقاييس حية محلي+إنتاج (3 جولات × 14 صفحة) — 3 اكتشافات جذرية |
| G4 | /design-review | 32 لقطة + 24 تحليل VLM + قياس تباين عددي — 4×P2 |
| G5 | /devex-review | TTHW مقاس فعليًا بنسخة نظيفة — DX 3.5/10 و13 نقطة احتكاك |
| G6 | /health | درجة مركبة 7.6/10 + أول قياس للصادرات الميتة (39.5%) |
| G7 | /investigate | التوكن أُبطل (المالك) — مسار بديل كشف 3 أعطال إنتاج حقيقية |
| S1-S4 | review specialists ×4 | 82 نتيجة (اختبارات/عقود/صيانة/تبسيط) بمخطط JSON موحد |

**موجات التنفيذ — 5 وكلاء + المنسّق:** W1a (أمن خلفي 12 بندًا) · W1b (بنية خلفية 6 بنود بأدلة حية) · W2 (وظيفي أمامي 9 بنود + تحقق حي 26/26) · W3 (أداء/بصري 5 بنود) · W4 (تطهير ~5,900 سطرًا) · W5 (**+104 اختبارات**).

## 2) أبرز الإغلاقات (بالأدلة)

### أمن (منهجية CSO — Phase 12: تحقق نشط + تنويعات)
| الخطر | الإغلاق | الدليل |
|---|---|---|
| 🔴 أسرار إنتاج Neon + SECRET_KEY في تاريخ git العام (منذ 2026-07-06) | **إجراء مالك إلزامي** (تدوير الثلاثة + scrub) — موثق في §5 | git show d7e5d8db:fb_dashboard/.env |
| HIGH: اسم مكرر عبر مستأجرين يقفل المصادقة (MultipleResultsFound) | تقييد البحث بـtid من JWT + fallback حتمي في get_current_user/login/WS | اختبار v10 حي |
| HIGH: editor يوقف بوت المنصة عبر مسار الوكيل | بوابة _ADMIN_ONLY_TOOLS (تصعيد مقفول) | test_v10_security |
| MED: /api/support/info يسكب SystemConfig | allowlist أربعة مفاتيح | اختبار يستحث مفتاحًا غير مسموح |
| MED: wallet-cap ثلاثي الاتجاه (خطر مالي) | توحيد المصدر: DB config في الباكند + قراءة دينامية في الواجهة | اعتراض بسقف 10 حيًا |
| MED: حقن HTML في PDF + SSRF أداة الصور | html.escape + _assert_safe_image_url (11 صيغة هجوم مرفوضة) | 12 اختبارًا بمعامل |

### أعطال إنتاج حقيقية (من تحقيق G7)
| العطل | الأثر قبل → بعد |
|---|---|
| SPA نطاق api.smart-link.ly هامدة منذ v6 (كل أصول JS/CSS = 404) | sync_next_static فقد مستوى static/ → **أُصلح السكربت + أُعيدت المزامنة** (109 ملفات، 4.3MB، مسارات صحيحة + خطوط محدّثة تكمل قلّم v9) |
| ترحيلات Alembic لا تعمل قط (import بلا fromlist) | تحذير مبتلع كل إقلاع → **INFO Alembic migrations applied** (001→010) |
| 3 نداءات Graph ضائعة بتوكن فارغ لكل تحميل لوحة | **صفر نداءات** (حارس has_global_fb_credentials) — زمن الطلب 0.59s→0.12s |

### أداء (قياس gstack /benchmark)
| المقياس | قبل → بعد (محلي حي) |
|---|---|
| خطوط لكل تنقّل | 210KB تُعاد → **cache-control: public, max-age=604800** |
| /api/public/stats في الرئيسية | 3 طلبات → **1 طلب** (single-flight 60s) |
| recharts في أول تحميل /demo | 352KB حاضر → **غائب** (رسم CSS للتبويب الافتراضي + lazy) |
| JS خام /demo | 987KB → **~635KB** |

### تطهير (منهجية simplification — كل حذف بجرد grep)
**~5,900+ سطرًا**: بناء Vite قديم كامل من جذر النشر (95 ملفًا/5.5MB كانت تُنشر على نطاق API بلا مرجع) · inbox_engine.py (390 + اختبارات النسخة الميتة) · migration.py · subscription_decisions.py · manual_tests/ (1,114) · 17 تابعًا ميتًا · صادرات الواجهة الميتة · 3 تبعيات (sse-starlette, react-is, autoprefixer).

### شبكة الانحدار (منهجية testing specialist)
**307 → 389 اختبارًا** (+104: انحدارات v10 الأمنية الـ38 + شبكة 401/403 على 52 مسارًا محميًا + توكنات فاسدة) · conftest.py حزام مشترك · **52.36% غطاء** (من 47%) · **عتبة 50% مفروضة في CI + gate_all** (لا تراجع ممكن).

### تجربة المطوّر (منهجية devex — TTHW مقاس)
README: أمر التشغيل المعطوب → الصحيح (`uvicorn runner:app --app-dir fb_dashboard`) · .env.example: التعليقات المضمّنة كانت تصبح قيمًا + DEBUG=false يفجر حراسة الإنتاج → أُعيدت كتابته (تعليقات مستقلة + DEBUG=true للتطوير) · LOCAL_API_PROXY موثق · CI: actions مثبتة بـSHA (بصمات محققة من GitHub API) + CODEOWNERS.

## 3) جدول القبول (الالتزام: أدلة لا تصريحات)

| # | المعيار | الحالة | الدليل |
|---|---|---|---|
| 1 | كل إصلاحات الخطة مُطبقة | ✅ | gate_all.sh **ALL GREEN** (ruff · pytest 389/1 · tsc 0 · build 41/41 · css/i18n/a11y/contrast) |
| 2 | صفر انحدار أمني | ✅ | test_v10_security.py (38) + test_v10_auth_negative.py (66) أخضر |
| 3 | SPA نطاق API حي | ⏳ إنتاج | محليًا: fb_dashboard/static/_next/static/ صحيح — **يُتحقق بعد الدفع** |
| 4 | الأداء محسّن | ✅ | live-verify: خطوط 604800 · stats=1 · recharts غائب (6/7 — السابع 400 توضيحي مقصود للعقد) |
| 5 | الكود الميت مطهّر | ✅ | 357 ملفًا تغيرت · −3,757 سطرًا صريحًا + 5.5MB ثنائية · 3 تبعيات أقل |
| 6 | DX تعمل بأمر واحد | ✅ | أمر README مصحح + env.example سليم (مسار G5 معاد تصميمه) |
| 7 | تباين AA صفحات المصادقة | ✅ | بوابة contrast خضراء + /60→/80 + --input الداكن ≥3:1 (مقاس) |
| 8 | commit واحد + إنتاج حي | ⏳ | هذا الالتزام — الدفع التالي ثم التحقق الحي |

## 4) المؤجل صراحةً (أمانة كاملة)

| البند | السبب |
|---|---|
| تفكيك runner.py/bot.py/PaymentDialog | M-L جهد — جولة مستقلة (خريطة S3 جاهزة بمواضعها) |
| توحيد ok() في 9 راوترات + الترقيم الأربعة | 30+ نقطة — جولة مستقلة |
| سقف تكلفة LLM لكل مستأجر + replay الويبهوك | قرار منتج / TENTATIVE منخفضة |
| اختبارات وحدة الواجهة (vitest) | بنية تحتية جديدة |
| حقول business_name/phone في التسجيل | قرار منتج (احتكاك مقابل اكتمال) |
| ميزانية JS الشاملة <500KB | يتطلب تقسيم حزم — الاتجاه موثق (900KB للوحة) |
| remaining backlog S1 (تلغرام double-tap، commerce HMAC، محركات broadcast/sequence) | الخطم التالي للغطاء نحو 56%+ |

## 5) ⚠️ إجراءات المالك العاجلة (لا تُغلق برمجيًا)

1. **فورًا — تدوير كلمة مرور Neon** (بيانات الإنتاج تسربت في تاريخ الريبو العام منذ 2026-07-06: git show d7e5d8db:fb_dashboard/.env) ومراجعة سجلات الوصول.
2. **تدوير SECRET_KEY + FERNET_KEY** في بيئة الإنتاج (يبطل الجلسات الحالية — مقبول) — بدونها تزوير JWT لمسؤول المنصة ممكن.
3. تدوير كلمة admin ثم scrub التاريخ (git filter-repo) + force-push — بعد الموافقة.
4. توكن Sentry أُبطل (رُصد 401 في v10) — توكن قراءة جديد إن أردت الرصد.
5. في Vercel: تفعيل «منع النشر عند فشل CI» (خارج الريبو) + SENTRY_AUTH_TOKEN للخرائط.

## 6) الحصيلة الرقمية

- **16 وكيلًا** (11 تشخيص + 5 تنفيذ) + المنسّق · **55 مهارة gstack** مثبتة ومستخدمة بمنهجياتها الحقيقية
- **357 ملفًا** تغيرت: +2,863/−3,757 سطرًا صريحًا + 5.6MB ثنائية محذوفة
- **389 اختبارًا** (من 307) · **52.36% غطاء** بعتبة CI 50% · **صفر مخالفات** في كل البوابات السبع
- 3 أعطال إنتاج حقيقية أُغلقت (أحدها عمره منذ v6) · 17 نتيجة أمنية أُغلقت أو قُيّدت بإجراء مالك
