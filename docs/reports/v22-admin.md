# تقرير المجال 6 — لوحة الأدمن الإدارية — v22 (W1-D6 + مساهمات FIX-G)

- **Task ID:** W1-D6 (تشخيص) → FIX-G (6f82b7e2 — حارس /admin + is_platform_admin) → W2-FIN (تجميع التقرير)
- **التشخيص:** `wave1-findings/W1-D6-admin-panel.md` (مصفوفة 41 مسارًا حية + E2E محلي كامل + مطاردة كلمة مرور أدمن الإقلاع)
- **لا إصلاح كود مطلوب للمجال** — الحكم العام W1-D6: **PASS** (الحدود محكمة)؛ FIX-G أغلق ملاحظة UX الوحيدة (§4).

---

## 1) حصر مسارات مسؤول المنصة واختبارها حيًا

**ما اختُبر:** 41 مسارًا عبر 11 ملف راوتر + logs_api.py — كلها تحت `require_platform_admin` (auth.py:121: role=admin ثم is_platform_admin — أدمن الإقلاع tenant 0 أو User.is_platform_admin=True).

**الدليل (كأدمن مستأجر مُسجَّل ذاتيًا v22w1d6adm2980، CSRF سليم — `d6_live_403_matrix.txt`):**
```
38 × 403 «هذه العملية تتطلب صلاحيات مسؤول المنصة» (بلا أي تسريب بيانات في الجسم)
 3 حالات مصمَّمة عمدًا (ليست ثغرات):
   GET  /api/admin/subscriptions?status=… → 200 بنطاق مستأجر الطالب فقط (v14-fix)
   DELETE /api/admin/tenants/{غيره} → 403 «لا يمكنك حذف مستأجر آخر»
   POST /api/admin/reset-password (مستخدم خارج المستأجر) → 404 scoped (إصلاح 2026-09-05)
مسارات مجهولة → 401 · /api/logs/* → 403 برسالة الحراس الخاصة · انحدار v15: /api/diagnostics/* السبعة كلها 403
```

**النتيجة:** **PASS** — لا تسريب، لا 500، حدود ذرّية صحيحة.

## 2) صفحات /admin/* في المتصفح (production)

**ما اختُبر:** كأدمن مستأجر — `/admin` تعمل (الطابور محصور بمستأجري: «لا توجد طلبات اشتراك»، بطاقة النبض لا تُعرض لغير tenant 0)؛ `/admin/settings` و`/admin/telegram` و`/admin/support` تعرض بطاقة خطأ عربية داخل الصفحة + «إعادة المحاولة» (لا صفحات فارغة/لا 500). لقطات: `D6-admin-*-blocked.png`.

## 3) E2E محلي كامل (كل إجراءات الأدمن تعمل وظيفيًا)

بأدمن منصة مزروع بكود التطبيق نفسه (SQLite محلي، `d6_seed_local_admin.py`):
- اعتماد دفعة → **PAID + plan_end +30 يومًا** (مرآة المستخدم والمستأجر)؛ رفض → REJECTED؛ **double-approve → 400** (الذرّية v9-A8)؛
- `/api/admin/platform/users` + تبديل `is_platform_admin` مرتين (العلم ينقلب في DB) + **صفّا audit_logs `platform_admin_toggle`** (مع actor/target)؛
- PATCH على أدمن الإقلاع (id=1) → **400 «حساب الإقلاع مسؤول منصة بالضرورة»**؛
- config write + سطر audit `admin_set_config`؛ `/api/repair` 200؛ alert-test؛ scheduler-check؛ bot restart؛ حذف مستأجر GDPR كامل (كأدمن إقلاع tenant 0) — والمستأجر المفوَّض لا يستطيع حذف غيره (403)؛
- الموافقة عبر المتصفح بجلسة أدمن حقيقية (طابور pending → verified → لقطات).

**النتيجة:** **PASS** — سطح الأدمن يعمل بالكامل.

## 4) كلمة مرور أدمن الإقلاع + ملاحظة /admin UX (أُغلقت)

- **كلمة مرور أدمن الإقلاع (id=1): غير قابلة للاسترجاع** — بذرة عشوائية `SB-…!ly` سُجلت مرة واحدة عبر log.warning عند أول إقلاع (~2026-09-03) وسجلات كل النشرات المبكرة منتهية الصلاحية («No logs found» — التحقق البصري أثبت أن مسار السجلات يعمل على أحدث نشر). **مسارات الاسترداد المشروعة موثّقة**: (1) «ahmad» (id=22, is_platform_admin=True) يستطيع `POST /api/admin/reset-password` لأي مستخدم بما فيه id=1؛ (2) POST /api/repair لا يعيد البذر إلا والجدول فارغ. **توصية**: تأكيد ملكية حساب 22 + تعيين مسؤول منصة مفوَّض ثانٍ احتياطي عبر `PATCH /api/admin/platform/users`.
- **ملاحظة UX (أُغلقت في FIX-G 6f82b7e2):** كانت قشرة `/admin` مرئية لكل أدمن مستأجر (AuthGuard يفحص role=admin فقط). الآن `/api/me` يعرض `is_platform_admin` وAuthGuard يبقي أدمن المستأجر خارج `/admin` في طبقة UX — بينما **الـAPI يبقى الإنفاذ الفعلي (403)** (طبقتا دفاع كما في W1-D10 §3).

## 5) حدود الانحدار

- v15 D-routes + مسارات الأدمن في كل السويتات التاريخية خضراء؛ بعد W2-FIN: **1014 passed / 0 failed**.

## 6) أدوات OSS

- استُعرضت أنماط RBAC/platform-admin في مشاريع مفتوحة (uvdesk/zammad لأدوار الدعم متعدد المستويات) — بقي الحارس الداخلي `require_platform_admin` (تكامل مباشر مع audit_logs والجلسات).
- سجل: `docs/reports/v22-tooling-used.md`.
