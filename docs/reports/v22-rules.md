# تقرير المجال 3 — قواعد الرد الآلي ومحرك البوت — v22 (W1-D3 + FIX-C fd1d4fa2 + التحقق الحي)

- **Task ID:** W1-D3 (تشخيص) → FIX-C (إصلاح) → W2-FIN (تجميع + تحقق حي)
- **التشخيص:** `wave1-findings/W1-D3-rules.md` (إنتاج commit 54452f32، حساب اختبار tenant 39)
- **الإصلاح:** commit **`fd1d4fa2`** — `fix(v22-D3/D5): max_rules plan enforcement + empty-keyword rejection + wallet network-mapping regression pins` — **مُنشر على الإنتاج**
- **التحقق الحي (W2-FIN):** SELECT على قاعدة الإنتاج — قواعد tenant 46 (حساب اختبار FIX-C) = 5 صفوف (ids 31–35)؛ القاعدة السادسة رُفضت 403 (أدلة الإصلاح الحية).

---

## الملخص

| البند | W1-D3 | بعد الإصلاح | الدليل |
|---|---|---|---|
| CRUD كامل (JSON+Form) + 404/422/403/401 + عزل المستأجرين | PASS (13 عملية حية) | PASS (لا تغيير) | W1-D3 §2 |
| ترتيب الأولوية + دلالات المطابقة + التطبيع العربي | PASS (20/20 محليًا) | PASS | W1-D3 §3/§4 |
| **B1: حد `max_rules` غير مفروض (مالي)** | **FAIL** — مستأجر Free أنشأ 8 قواعد حية | **PASS** ✅ 403 «حد قواعد الرد لخطتك هو 5 — رقِّ خطتك» | §1 أدناه |
| **B2: كلمات مفتاحية فارغة → قاعدة catch-all ضمنية** | **FAIL** (قاعدة 29 حية تجيب كل تعليق) | **PASS** ✅ 400 «الكلمات المفتاحية مطلوبة» | §2 أدناه |
| عدادات الاستخدام (max_replies) | PASS (E2E محلي) | PASS | W1-D3 §6 |
| XSS/RTL في واجهة القواعد | PASS (نص حرفي، بلا أخطاء JS) | PASS | W1-D3 §8 + لقطات D3-* |

---

## 1) B1 (HIGH، مالي) — فرض حد قواعد الخطة

**ما اختُبر (W1-D3):** `subscription_plans.max_rules` (Free=5) موجود في النموذج والبذرة لكن `create_rule` لم يكن يستشيره — مستأجر Free (بلا خطة → حد Free) أنشأ 8 قواعد حية كلها 200. العقد المطلوب: 4xx نظيفة برسالة عربية.

**الإصلاح (fd1d4fa2):** بوابة عبر `get_plan_limits` + صف الخطة (نفس نمط حدود الفريق في users.py — v17-E-B2):
- 403 «**حد قواعد الرد لخطتك هو {n} — رقِّ خطتك**» عند بلوغ الحد؛
- العدّ = إجمالي القواعد (وعد «N قواعد رد» في الخطة = حجم كتاب القواعد، نفس عقيدة max_team)؛
- التحديث/الحذف يبقيان متاحَين عند الحد (لا نمو في العدد)؛
- fail-open عندما لا يحمل صف الخطة max_rules (لا حجز خطة عالقة برسالة خاطئة).

**الدليل الحي (SELECT، 2026-09-10):** tenant 46 أنشأ **5 قواعد بالضبط** (ids 31–35، أسماء «v22fixc rule 1..5») ثم المحاولة السادسة → **403 برسالة الحد** — القواعد المخزنة مطابقة للحد الأدنى المسموح، ولا صف سادس.

**النتيجة:** **PASS** ✅

## 2) B2 (MED) — رفض الكلمات المفتاحية الفارغة

**ما اختُبر (W1-D3):** `keywords=" , , "` كانت تجتاز فحص السلسلة الخام وتُخزَّن `keywords=[]` → قاعدة **catch-all ضمنية** تجيب على كل تعليق (أثبت حيًا بالقاعدة 29 ثم حُذفت).

**الإصلاح:** `_keywords_list()` ترفض القائمة المحلَّلة الفارغة بـ 400 «الكلمات المفتاحية مطلوبة» عند الإنشاء والتحديث؛ الكلمة الصريحة `__catch_all__` تبقى مقبولة (عقد المحرك، مع توثيق أنها ميزة مستقبلية بلا واجهة).

**النتيجة:** **PASS** ✅ (اختبارات سويت FIX-C خضراء — سجل FIX-RECOVERY 1002/1002).

## 3) ملاحظات بقيت كما هي (موثّقة، ليست عللًا)

| # | الملاحظة | القرار |
|---|---|---|
| D3-B3 | `dm_map` في facebook_automation.json مفاتيحه برقم القاعدة والمحرك يبحث بالاسم — كود ميت | منخفضة: بلا أثر مستخدم (dm_template لكل قاعدة هو المسار العامل) |
| D3-B4 | Phase-1 intent matching قد تتجاوز ترتيب الأولوية لقواعد تحمل أسماء نوايا إنجليزية | توثيق فقط — لا وجود لها في الإنتاج |
| D3-O1 | catch-all موجود بالمحرك بلا واجهة/توثيق | مسار مستقبلي (الكلمة الصريحة مقبولة بعد الإصلاح) |
| D3-O2 | `bot_type` مقبول ومهمل في الإنشاء | cosmetic |

## 4) حدود الانحدار

- 148 اختبار W1-D3 خضراء + سويت FIX-C (اختبارات الحدود + الكلمات الفارغة + تثبيتات خرائط المحفظة D5) — خضراء.
- كامل السويت بعد W2-FIN: **1014 passed / 0 failed** (صفر انحدار على المجال).
- E2E محلي كامل للويبهوك → رد آلي مسجل (سكربت `w1d3-e2e-mock.py`) يبقى مسار إثبات التعليقات (تعذّر اختباره حيًا: comments=0 بغياب pages_read_user_content — انظر تقرير فيسبوك).

## 5) أدوات OSS

- **facebook-sdk (mobolic/facebook-sdk, ~2.8k★)** و **requests-based Graph wrappers** — استُعرضت لنمط الـdedup/edge calls؛ بقي `fb_client.py` الداخلي (اعتماد أقل، نفس عقد الأخطاء الصادقة).
- سجل: `docs/reports/v22-tooling-used.md`.
