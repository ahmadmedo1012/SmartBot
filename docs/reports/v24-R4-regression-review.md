# v24-R4 — Fresh-Eyes Regression Review (READ-ONLY)

- **Reviewer:** R4-REGRESSION (Task v24-R4)
- **Scope:** commit `300cb150` "feat(v24): massive diagnostic + repair round" (HEAD), 73 files, +5951/−565. Uncommitted round-2 WIP (messages/page.tsx, instrumentation-client.ts, auth.py, api_cache.py, redis_cache.py, telegram.py, payments/*) was **excluded** from judgment but noted where it interacts.
- **Method:** per-file `git show HEAD --`, contract reads of surrounding unchanged code, targeted greps, and read-only verification runs (see §4).

---

## 1. Findings (hostile-review pass)

| # | Severity | Location | Issue | Evidence | Recommended fix |
|---|----------|----------|-------|----------|-----------------|
| F1 | **HIGH (security)** | `fb_dashboard/_rate_limit.py:88-104` (`client_ip`), used by `app/middleware.py:70-76` | **Left-most-public XFF selection is client-spoofable behind Vercel.** Vercel *appends* the real client IP to `X-Forwarded-For`; client-supplied hops are the *leftmost* entries. `client_ip()` scans left→right and returns the first public IP → a client that sends `X-Forwarded-For: 8.8.8.8` controls its own bucket key. Consequences: (a) **rate-limit bypass** — rotate fake public IPs to get a fresh `mutate:` bucket per request (defeats the 30/60s cap); (b) **targeted cross-user lockout** — spoof the victim's IP as the leftmost entry to burn *their* bucket (30/60s), i.e. the exact cross-user-429 class v24 set out to fix, now weaponizable. Default deployments (no `VERCEL`, no `SMARTBOT_TRUST_XFF=1`) are safe — XFF is ignored. | Code order: `for hop in xff.split(","): if public: return hop`. New test `tests/test_v24_c4_backend.py::test_client_ip_vercel_takes_leftmost_public_ip` **pins the spoofable behavior**. | Iterate from the **right** (rightmost entry = appended by the trusted proxy), or prefer `x-vercel-forwarded-for`/`x-real-ip`, or strip N known trusted hops. Update the pinning test. |
| F2 | **HIGH (privacy / cross-user)** | `lib/query-persist.ts:190-197` + `DashboardShell.tsx:52` + `lib/csrf-client.ts:80-84` | **Persisted query cache survives forced logout; next login as a different user (same tab) hydrates the previous user's dashboard data.** Two dirty paths: (a) **401 mid-session** → `window.location.replace("/login?…")` (full page nav) — `sessionStorage` *survives* page replacement; `clearQueryPersistedCache()` is only called on the *manual* logout button. (b) **Debounce straggler**: `flush` is debounced 800 ms; `handleLogout` clears storage *then* `router.push("/login")` — if a poll event landed <800 ms before logout and the RSC navigation takes >800 ms (normal on 3G), the pending `flush` **re-writes** the previous user's cache to sessionStorage after the clear. `['me']` is excluded (identity safe), but inbox conversations/messages, comments, posts, team, tickets etc. are persisted (≤500 KB). Exposure is same-tab and bounded by each query's staleTime (default 30 s; several pages set 15–60 s), but it is real cross-account data display. | `Grep clearQueryPersistedCache` → only `DashboardShell.tsx:52`. `csrf-client.ts:80-84` redirect does not clear. `query-persist.ts:163-180` debounce + `attachQueryPersister` detach only cancels on unmount (race window). | Call `clearQueryPersistedCache()` (a) inside apiFetch's 401 redirect, and/or (b) on `/login` page mount (covers both paths + straggler), and (c) have `handleLogout` detach/disable the persister before `router.push`. |
| F3 | **MEDIUM (a11y)** | `messages/page.tsx:708-723` (scroller `role="log" aria-live="polite"`) + `:759-772` | **The live region wraps the whole 60-bubble scroller incl. the load-earlier button, skeletons and error card.** Poll *diffs* are fine (keyed DOM reuse → no mutation → silence; one appended node → one announcement — the design intent). But (a) **thread switch** replaces the entire region content and (b) **load-earlier** injects 60 bubbles at once — many SR engines (NVDA/VoiceOver with default `aria-relevant="additions text"`) will queue/announce a burst of up to 60 additions ("announce the whole log"), a genuine SR-disaster risk on open/switch, not on every poll. Tests only pin the *attributes*, not the announcement behavior. | Region contains `{hiddenCount>0 && <Button …/>}` and skeleton/error branches. `MessagesPage.test.tsx:510` pins only `toHaveAttribute("aria-live","polite")`. | Scope `role="log"` to the bubbles-only container (exclude the button/skeletons) *and* announce "N رسائل جديدة" via a small visually-hidden polite status strip; or set `aria-busy` during thread swap. |
| F4 | **MEDIUM (UX/history)** | `messages/page.tsx:410-438` (`popstate` handler + `closeThread`) | **`pushDepthRef` ledger drifts after forward traversals → back can reopen a closed thread.** `popstate` cannot tell back from forward, and the handler *decrements on both*: `pushDepthRef = c ? max(0, d-1) : 0`. Sequence: open A (d1) → open B (d2) → back → back (list, d0) → **forward** (thread A, d0) → **forward** (thread B, d=max(0,-1)=0) → back row (replaceState; forward entry keeps `?c=B`) → hardware back → **thread B reopens** after the user "closed" it. Narrower variant: a thread opened *over a deep-linked/refreshed `?c=` entry* with depth>0 pops back into that `?c=` entry (reopen). Low frequency, confusing when hit. | Handler at 410-418; `closeThread` at 432-438 chooses `history.go(-depth)` vs `replaceState` purely from the possibly-drifted ledger. | Stamp a marker into `history.pushState({...}, …)` and compare `event.state` in popstate (direction + ownership), or track `window.history.state` index deltas; treat unknown `?c` entries as depth 0 and, on `closeThread`, also `replaceState` when the target entry is not list-state. |
| F5 | **MEDIUM (cost abuse partially closed)** | `routers/ai.py:148-160` (`/api/agent/interpret`) | **The daily AI budget does not cover the agent endpoint.** All four `/api/ai/*` mutation routes call `_ai_daily_budget`; `/api/agent/interpret` — an LLM-driving surface — has the `require_role("editor")` gate but **no 30/day cap** (and the agent engine can make multiple provider calls per request). The H2 "AI cost abuse" fix is 4/5 surfaces. | ai.py: line 57/76/98/115 have `await _ai_daily_budget(...)`; 148-160 does not. | Add `_ai_daily_budget(db, current_user)` to `agent_interpret` (or cap inside the agent engine per interpretation). |
| F6 | **MEDIUM (documented gap → round-2 handoff)** | `routers/auth.py:135, 262, 444, 491`; noted in `_rate_limit.py` docstring NOTE | **Login/register/change-password limiters still key on `request.client.host`.** On Vercel every user shares ONE `login:` bucket (10/60s) → the cross-user 429 lockout the middleware fix addresses persists on the **login path** (middleware explicitly exempts `/api/login`+`/api/register`). Not a regression (pre-existing), but the v24 claim "cross-user 429 lockouts fixed" is only true for `mutate:` buckets. Round-2 is editing `auth.py` right now — flag to them. | auth.py:135 `ip = request.client.host …f"login:{ip}"`; middleware exemption list `/api/login`, `/api/register`. | Adopt `client_ip()` in those four spots **after fixing F1** (else the spoofable leftmost IP lands in the login limiter too). |
| F7 | **MEDIUM-LOW (table growth / write amplification)** | `_rate_limit.py:11-31` (`check_rate_limit`) | **One row per AI call, and one row per *rejected* attempt.** `db.add` happens before the count, so a capped user hammering `/api/ai/*` writes a `rate_limit_entries` row on every 429 (unbounded while under attack); cleanup is per-key on next check (dormant keys linger). Pre-existing design, but v24 put it on a hot path (every AI call = INSERT+COMMIT before the LLM call). | `check_rate_limit` body: `db.add(...); await db.flush(); await db.commit();` then count. | Aggregate counter row per key (UPDATE … window_end), or skip the insert once the window is already exceeded; add a periodic global purge of expired rows. |
| F8 | **LOW (privacy/storage)** | `messages/page.tsx:70, 214-246` | **`draft:<conversationId>` localStorage keys never expire, are not user-scoped, and are not cleared on logout.** On a shared computer user B can hydrate user A's unsent reply text (same conversation id), and deleted conversations leave orphan keys forever. | `DRAFT_KEY_PREFIX = "draft:"`; no expiry, no logout cleanup anywhere. | Namespace by user id (`draft:<uid>:<cid>`) or clear `draft:*` in the logout handler; opportunistic purge of orphans. |
| F9 | **LOW (i18n cosmetics)** | `app/errors.py:19-25` (`validation_handler`) | Out-of-range query params (`month=13` → 422) render as **"الحقل 'query.month' مطلوب"** ("required") — misleading for range violations the new calendar bounds intentionally produce. | Handler joins `f"الحقل '{field}' مطلوب"` for every error type. | Include `e["msg"]`/type distinction (required vs `le=12` violation). |
| F10 | **LOW (ops race)** | `alembic/versions/017_v24_datalayer.py:153-167`; `app/startup.py:206-217` | `op.create_index(_OFFERCLAIM_UQ …)` has **no `IF NOT EXISTS`** and the inspector guard is TOCTOU; two serverless instances racing boot → second fails with "already exists", logged as `Alembic upgrade skipped: …` (warning) and retried idempotently next boot. Self-healing, but a boot can log a scary warning and the version table stays unstamped until a clean run. Also `information_schema.columns` is not schema-filtered. | 017:165-167 vs the `CREATE INDEX IF NOT EXISTS` belt used for the five hot indexes; startup catches `Exception` → warning. | Use `sa.text("CREATE UNIQUE INDEX IF NOT EXISTS …")` (PG) or wrap in a dialect-aware exists check; add `AND table_schema = current_schema()`. |
| F11 | **LOW (visual, accepted trade)** | `components/ui/switch.tsx:34` | The 44×44 real-box switch makes dense rows (autoreply/tools/telegram config) ~26 px taller and 12 px wider; `min-w-11` enlarges inline rows. Intentional (touch contract); the old `after:-inset` hit-area *bleed over neighbors* is gone — a net improvement. No fixed-height clipping found (usages are `items-center` flex rows). | Old: 18.4 px-tall button + pseudo hit area peaking 34 px and overlapping neighbors; new: contained 44×44. | None needed; verify visual density on `tools` page at 375 px during round-2 QA. |
| F12 | **INFO (round-2 integration)** | working tree (uncommitted) | `tests/test_phase_b_payments.py::test_upload_receipt_ok` **fails in the working tree** (receipt file not written) but **passes at committed HEAD** (verified in an isolated `git worktree` — full suite: **1043 passed**). The failure is round-2 WIP (auth.py/wallet/plans/api_cache are modified), not v24. Likewise the 3 failing vitest cases are `v24-R1` (round-2, uncommitted) — all committed v24 pins pass. | Worktree run output; `git status --short`. | Round-2 agents: check the wallet upload path against your auth/api_cache changes. |

---

## 2. Verdict on the 13 checklist areas

1. **messages/page.tsx** — Mostly SOLID with 3 findings: F3 (aria-live region too broad), F4 (history ledger drift/forward-stack reopen), F8 (draft storage hygiene). Verified-good: push/pop/back-row basics, chat-focus body attr cleans up on every unmount path (effect cleanup, no leak), windowing + scroll-anchor math (`pendingAnchorRef` captured before prepend, delta restored in `useLayoutEffect` — correct; pinned by test), draft isolation/hydration race-free (updater is synchronous per selectedId; mid-debounce switch files under the captured id; clear-on-send kills pending timer), optimistic-send rollback, no XSS via conversation ids (React text nodes; opaque storage keys). Delete-conversation-while-open: no delete UI on this page; a 404 from another surface lands in the retryable error branch — acceptable; orphan draft noted (F8).
2. **AuthGuard/useMe/QueryProvider** — Gate semantics preserved (children only on real user + role pass; 401/network → `/login?redirect=`). `['me']` excluded from persistence ✓; in-memory client dies with layout unmount on manual logout (login is outside the dashboard layout) ✓. **But F2**: 401-forced logout and the debounce straggler leak the persisted cache to the next same-tab user. staleTime 5 min + 60 s interval interplay is sound (interval refetches while observed; hidden tabs don't poll). Demo interceptor contract preserved (only `/demo` passes `onNavigate`; prefetch disabled there — correct).
3. **MobileBottomNav/AdminSidebar** — Focus-trap selector is **NOT truncated**: `'a[href], button:not([disabled]), [tabindex]:not([tabindex="-1"])'` — links are included, Tab cycling works with `<Link>`. Active matching: no `trailingSlash`/`basePath` config; `/dashboard` exact + `startsWith` for nested (desired). Closed sheet is `translate3d(0,100%,0)` off-screen so IntersectionObserver never fires → prefetch claim holds; bar items prefetch while visible. `aria-current="page"` added ✓.
4. **switch.tsx** — See F11. RTL thumb math unchanged (translate relative to thumb, logical flex-start flip + `rtl:-translate-x`); track/thumb sizes preserved; focus ring now wraps the 44px box.
5. **pipeline.py savepoint** — **VERIFIED-SAFE.** `IntegrityError` imported (pipeline.py:25); `begin_nested` runs inside the autobegin transaction (the preceding SELECT); loser path re-reads the committed winner (PG READ COMMITTED / SQLite single-writer guarantee the conflicting row is committed); `c is None → raise` guarded; outer except now rolls back before warning — session never left in PendingRollback. Tests: race + commit-failure cases in `test_v24_c4_backend.py` pass.
6. **Migration 017 on legacy PG** — **VERIFIED-SAFE with F10 note.** `ALTER TYPE … USING col AT TIME ZONE 'UTC'` is the correct timestamptz→naive-UTC conversion (keeps UTC wall clock = repo convention); dependent indexes are auto-rebuilt by PG (no views/FKs on those columns); the `now()` default rewrite matches 003's `server_default=sa.text("now()")` (004's tz columns carry no defaults — nothing missed); dedup DELETE-before-unique is safe under the documented single-writer boot. Full-rewrite lock is acceptable at lifespan. Chain 001→017 ≡ create_all confirmed by tests.
7. **publisher tz normalization** — **VERIFIED-SAFE.** Naive input → `tzinfo is None` → untouched (required no-op) ✓; aware → `astimezone(UTC).replace(tzinfo=None)` ✓; `Z`/`+0000` handled. Past-date 400 is NEW behavior: frontend `scheduled` page surfaces it via `unwrapApi` → `brandedToast.error(e.message)` (Arabic detail) ✓ — no raw-500 regression path.
8. **AI role gate + daily cap** — Role hierarchy `admin 3 ≥ editor 2 ≥ viewer 1`; no `owner` role exists in the app (models.py: `admin, editor, viewer`) → owner/admin pass ✓. Cap key `(tenant,user)` rolling-24 h window (no midnight boundary, UTC) — message says "غداً" (cosmetic). Cap is env-tuned at import (fine for ops). **F5** (agent endpoint uncapped) + **F7** (row-per-call growth incl. rejected attempts). 403/429 surface as Arabic `detail` through ApiError → comments dialog/toasts ✓.
9. **client_ip XFF** — **F1 (HIGH)**: leftmost-public selection spoofable behind Vercel; port stripping and IPv6 `[::1]:443` handling are correct; private/loopback hops skipped; default (no trust flag) is safe (header ignored).
10. **Raw-body 422s** — **VERIFIED-SAFE.** Body 422s return `{"detail": "<Arabic string>"}` via `_json_body`/`_required_key`; FastAPI query-param 422s are normalized to the same string shape by `validation_handler` (app/errors.py). Frontend forms (sequences/flows/calendar/team/scheduled) uniformly do `unwrapApi` → `ApiError.detail` → toast. Cosmetic F9 on "required" wording for range violations.
11. **Broadcast estimate endpoint** — **EXISTS — VERIFIED.** `POST /api/broadcasts/estimate` (broadcasts.py:174, since v15, untouched by v24) behind `require_role("editor")`, expects `{segment_filters, platform_filter}` — exactly what the dialog sends; returns `ok({count})` matching `unwrapApi<{count}>`. Detail route returns both filter fields. Dialog keeps confirm disabled while loading/errored, retry without blind send. **Not critical — contract intact.**
12. **Contrast token changes** — **VERIFIED-SAFE.** All `--c-ember` consumers (button.tsx flame variant, PlanSelector, StepIndicator) pair ember gradients with `text-espresso` — raising ember lightness *improves* those pairs; no white-on-ember consumers found. `--input` consumers are borders (`border-input*`) and the switch's unchecked track — darker values are the intended 1.4.11 strengthening; `--border` deliberately untouched (dividers stay decorative). Pinned by `ContrastTokens.test.tsx` (38 pairs) + extended `check_contrast.mjs` (hex parsing, per-pair minimums).
13. **Vitest pins** — **Meaningful, not snapshot noise.** The +6 MessagesPage pins assert real contracts: URL push + popstate back + chat-focus cleanup, deep-link mount, back-row no-exit, windowing (60 newest, batch prepend, count label), draft survive-blur+remount / clear-on-send / keep-on-fail. Gaps: no pin for forward-traversal ledger drift (F4) and attribute-only aria-live pin (F3). AuthGuard tests cover 401-on-load, platform-admin gate fail-closed, skip-CSRF toast. ContrastTokens pins compute real WCAG ratios.

---

## 3. VERIFIED-SAFE areas (coverage map for the orchestrator)

- messages: thread↔URL basics, back-row, chat-focus cleanup, windowing+anchor, drafts isolation/hydrate/clear, optimistic send/rollback, list semantics (`<ul role=list>` + plain buttons).
- nav: Link refactor (focus trap incl. `a[href]`, active matching, prefetch visibility, demo interceptor), `#page-content` + nav-hide CSS selector literal match.
- switch: 44px box, RTL math, hit-area bleed removed.
- backend: pipeline CRM savepoint race; webhooks JSON-guard (400 post-signature, no FB retry storm); users 409-on-race (team toast); sequences/flows/calendar 422 + calendar bounds; publisher tz/past-400; step_count single grouped query; offer_engine DB dedup (savepoint + next-candidate loop + degrade-on-write-failure); sequence due-scan one-query rewrite (dialect-aware due filter; NULL/dup-step/missing-subscriber semantics preserved; tenant_id carried); migration 017 (heal conversion, defaults, idempotency, chain≡create_all).
- gates at committed HEAD (isolated worktree): **pytest 1043 passed / 0 failed**; **tsc --noEmit clean**; **vitest 398/398 for committed pins** (working-tree failures are uncommitted `v24-R1` round-2 tests); build/contrast gates reported green in C-reports.

## 4. Verification runs performed (read-only)

- `git worktree add /tmp/r4/head-check HEAD` → `python -m pytest -q -k "not live"` → **1043 passed**; removed worktree afterwards.
- Working-tree `pytest -q -x` → `test_upload_receipt_ok` FAIL (round-2 WIP; passes at HEAD — F12).
- Working-tree `npx vitest run` → 397/398; only `v24-R1` (round-2) cases fail. `npx tsc --noEmit` → clean.
- Contract greps: estimate endpoint, `clearQueryPersistedCache` call sites, role hierarchy/roles, `--c-ember`/`--input` consumers, sheet-panel transform, `#page-content`, XFF trust-flag defaults, 003/004 server defaults.

## 5. Recommended next actions (priority order)

1. **F1** — flip `client_ip()` to rightmost-entry resolution (or Vercel header) + update the pinned test; coordinate with the round-2 auth.py agent (F6) so the login limiter adopts the *fixed* helper.
2. **F2** — clear the persisted cache on the 401 redirect and/or `/login` mount; make logout detach the persister before navigating.
3. **F3/F4** — messages round-2 (currently in flight in that exact file): scope the live region to bubbles-only + status strip; make the depth ledger direction-aware via `history.state` markers.
4. **F5** — add the daily budget to `/api/agent/interpret`.
5. F7–F10 — hygiene backlog (aggregated limiter row, draft namespacing, validation wording, `IF NOT EXISTS` on 017's unique index).
