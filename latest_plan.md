# خطة SmartBot الشاملة — إعادة هيكلة عميقة نحو منتج عالمي المستوى

> **تاريخ الفحص:** 2026-09-03 | **المصدر:** فحص مباشر لمستودع `ahmadmedo1012/SmartBot` (commit `ce1eae1d`) عبر `git clone` + `grep` + قراءة كود فعلية — لا تخمين ولا افتراضات.
> **الحالة السابقة للمرجع:** خطة يوليو (`docs/remediation-plan.md`, 197 ملاحظة) **جزئياً قديمة** — المشروع مرّ بإعادة بناء كاملة تحت اسم `world-class-rebuild` (PR #5، دُمج اليوم 09:39، وتلته 7 commits إصلاح حتى 15:06). هذه الخطة تحل محلها ولا تكررها.

---

## 0. منهجية الفحص (كيف تم التحقق)

كل نتيجة أدناه إما (أ) مقتبسة من سطر كود فعلي مع رقم السطر، أو (ب) ناتجة عن أمر `grep`/`diff` فعلي شغّلته على نسخة حية من المستودع. لم أعتمد على أي وصف سابق في الذاكرة أو التقارير التاريخية دون تحقق مباشر. حيث وجدت أن مشكلة قديمة (من يوليو) **قد أُصلحت فعلاً** في إعادة البناء الأخيرة، ذكرت ذلك صراحة بدل تكرارها كمشكلة — لأن خطة زائفة عن مشاكل غير موجودة تضيع وقت الوكلاء بلا فائدة.

**قاعدة إلزامية لأي وكيل ينفذ هذه الخطة:** أي بند مؤشَّر `[VERIFY]` يعني أنني رصدت مؤشراً قوياً لكن لم أتحقق منه بعمق كافٍ — يجب على الوكيل التحقق بالفحص المباشر (grep/قراءة كود/تشغيل فعلي) **قبل** الإصلاح، وإرفاق دليل الفحص في تقرير التسليم. **ممنوع الإصلاح بالتخمين.**

---

## 1. الحقائق الحرجة الموثقة (Executive Findings)

### 1.1 عدم اتساق عقد الـ API عبر كامل الباك-إند — أخطر مشكلة بنيوية واحدة

فحصت جميع الـ 35 راوتر في `fb_dashboard/routers/` بحثاً عن نمط `{"success": ..., "data": ...}` مقابل إرجاع dict خام:

| الحالة | عدد الراوترات | أمثلة |
|---|---|---|
| **يستخدم الغلاف بالكامل** | 2 فقط | `payments.py` (11/11)، `telegram_config.py` (11/11) |
| **يستخدم الغلاف جزئياً** | 4 | `auth.py` (9 من 15)، `dashboard_stats.py`، `ai.py`، `plans_config.py` |
| **لا يستخدم الغلاف إطلاقاً — dict خام** | **29 راوتر** | `admin_routes.py`, `analytics.py`, `bot.py`, `facebook_routes.py`, `inbox.py`, `rules.py`, `scheduled_posts_routes.py`, `sequences.py`, `broadcasts.py`, `calendar_routes.py`, `crm_routes.py`, `diagnostics.py`, `flows.py`, `offers_routes.py`, `publisher_routes.py`, `replies.py`, `reports_routes.py`, `subscribers_tags_routes.py`, `team_routes.py`, `templates_routes.py`, `users.py`, `webhooks.py`, `widgets_routes.py`, `brand_routes.py`, `alerts_routes.py`, `health_alerts_routes.py`, `commerce_routes.py`, `team_routes.py` |

**الأثر:** كل استدعاء `fetch` في الواجهة الأمامية يتعامل مع شكلين مختلفين تماماً للاستجابة حسب أي endpoint يستدعيه، بدون أي طبقة توحيد. هذا هو الجذر الحقيقي وراء أي "تشوه" أو تصرف غير متوقع في البيانات المعروضة — وليس خطأً بصرياً بل **عقد بيانات مكسور بنيوياً في 83% من الـ API**.

### 1.2 تكرار حرفي لملفات كاملة (تكرارات مؤكدة)

| الملف | الحالة | الخطورة |
|---|---|---|
| `fb_dashboard/frontend/src/app/dashboard/[...slug]/DashboardPage.tsx` | **كود ميت 100%** — لا يستورده أي ملف (تحققت بـ `grep -rn "DashboardPage"`)، لكنه نسخة كاملة (182 سطر) من `page.tsx` المجاور (182 سطر) بخرائط API **متضاربة**: `messages`→`/api/inbox/conversations` في الملف الميت مقابل `/api/messages` في الملف الحي؛ `autoreply`→`/api/rules` في الميت مقابل `/api/admin/rules-categories` في الحي | **حرج** — أي وكيل مستقبلي (بشري أو AI) قد يعدّل الملف الخطأ ظناً أنه المسار الفعلي |
| `fb_dashboard/routers/payments.py.bak` | نسخة احتياطية **مُتتبَّعة في git** (لا يجب أن تكون كذلك) من إصدار سابق لـ `payments.py` قبل تطبيق غلاف `{success,data}` — 11 اختلافاً بينهما | **متوسط** — تلوث بصري للمستودع + خطر استيراد بالخطأ |

**قاعدة عامة للوكيل:** هذان مجرد عينتين وجدتهما بفحص عيّنة. **المرحلة الأولى من التنفيذ (القسم 3، Track C) تُلزم بمسح شامل آلي لكامل المستودع** — لا يُفترض أن هذين الملفين هما كل التكرار الموجود.

### 1.3 صفحات "اكتمال مزيّف" — تدّعي العمل لكنها لا تعمل فعلياً

| الصفحة | الملف | الدليل |
|---|---|---|
| الإشعارات | `dashboard/notifications/page.tsx` | كل الحالة في `useState` محلي فقط (سطر 17-19). **لا يوجد أي `fetch`**. النص المعروض للمستخدم في الصفحة نفسها (سطر 64-66) يقول حرفياً: *"تُحفظ إعداداتك تلقائياً وتُطبق على جميع المنصات"* — **هذا كذب واجهة (UI lie)**: لا حفظ يحدث، أي تحديث للصفحة يمسح كل التفضيلات |
| الدعم | `dashboard/support/page.tsx` | بيانات تواصل ثابتة ومزيّفة بوضوح: `support@smartbot.ly`، هاتف `0210000000`، واتساب `0920000000` (أنماط أرقام placeholder واضحة) — لا `fetch` واحد في الصفحة |
| Fallback الديناميكي | `[...slug]/page.tsx:99` | نص "هذه الصفحة قيد التطوير" لا يزال يظهر لأي مسار في القائمة الجانبية (23 عنصر) لا يملك صفحة مخصصة |

### 1.4 مكتبة رسوم بيانية مثبّتة لكن **غير مستخدمة إطلاقاً**

`package.json` يعلن `"recharts": "^3.9.2"` كـ dependency. فحص شامل (`grep -rl "from \"recharts\""`) على كامل `src/` أعاد **صفر نتائج**. بدلاً منها، `dashboard/page.tsx` و`dashboard/analytics/page.tsx` و`demo/page.tsx` ترسم الرسوم البيانية يدوياً بـ `<div style={{height:...}}>`. هذا يفسر مباشرة أعمدة الرسم البياني "الفارغة/غير الواضحة" التي رصدتها في لقطة الشاشة القديمة للوحة التحكم — حل جاهز موجود في `node_modules` ولا أحد يستخدمه.

### 1.5 لا يوجد أي تنقّل على الجوال في لوحة التحكم — فجوة وظيفية خطيرة

`DashboardShell.tsx` سطر 22: القائمة الجانبية مُغلّفة بـ `hidden md:block` فقط — أي **مخفية بالكامل على الجوال**. فحصت `components/layout/` و`app/dashboard/` بحثاً عن `md:hidden` أو `MobileNav` أو `BottomNav` أو `Sheet`/`Drawer` — **لا يوجد أي منها**. النتيجة: أي عميل يدخل من هاتفه بعد تسجيل الدخول يرى محتوى الصفحة الحالية **بدون أي وسيلة للتنقل** بين الأقسام الـ23. هذا يمس مباشرة طلبك الأساسي ("يدخل العميل... يصل إلى الخدمات... بدون مشاكل") لأن الغالبية العظمى من عملاء ليبيا يستخدمون الجوال.

### 1.6 توصيات المكتبات مفتوحة المصدر السابقة — تصحيح ضروري

كنت قد طلبت سابقاً الاستفادة من `LiveCharts2` و`scroll-craft` و`facebook-mcp-server`. تحققت من الثلاثة بالفحص المباشر (استنساخ + قراءة):

| المكتبة | الفحص الفعلي | الخلاصة |
|---|---|---|
| `Live-Charts/LiveCharts2` | ملفات المشروع: `LiveCharts.WPF.slnx`, `LiveCharts.Avalonia.slnx`, `LiveCharts.Blazor.slnx`, `LiveCharts.MAUI.slnx`, `LiveCharts.WinForms.slnx` — **مكتبة C#/.NET بالكامل** | **غير متوافقة تقنياً مع Next.js/React** — لا يمكن استخدامها مباشرة. **البديل الصحيح: تفعيل `recharts` الموجودة فعلاً في `package.json` والمستخدمة صفر مرة (انظر 1.4)** بدل استيراد مكتبة من نظام تقني مختلف تماماً |
| `nateherkai/scroll-craft` | README يوضح أنه **مهارة Claude Code (Skill)** لبناء صفحات هبوط سينمائية بتفاعل تمرير (scroll-driven) — ليست مكتبة JS تُستورد | **مناسبة فقط لصفحة الهبوط الرئيسية (`app/page.tsx`) وصفحة التسعير `pricing/`** — التسويقية العامة، **وليست** للوحة التحكم الداخلية (تحتاج كفاءة وسرعة تصفح لا سينمائية) |
| `HagaiHen/facebook-mcp-server` | Python بالكامل (`server.py`, `facebook_api.py`, `manager.py`) — خادم MCP يكشف ~30 أداة جاهزة (نشر منشورات، إدارة تعليقات، رسائل مباشرة، إحصائيات Insights، حذف/إخفاء جماعي) قابلة للاستدعاء من Claude مباشرة | **متوافقة تماماً** مع الباك-إند الحالي (FastAPI/Python). **لكن**: `requirements.txt` الحالي **لا يحتوي** حزمة `mcp` ولا `anthropic` — هذا دمج جديد كلياً، ليس تفعيل شيء موجود. يُعامل كمسار عمل منفصل ومعزول (Track G) لا كإصلاح سريع |

### 1.7 أمور من خطة يوليو الأمنية — تحققتُ: **تحسّنت فعلاً بعد إعادة البناء**

لتجنب تكرار عمل منجز، تحققت مباشرة من 3 بنود أمنية حرجة من الخطة القديمة:

- ✅ **Webhook HMAC**: `runner.py:813-814` الآن **يرفض** الطلب صراحة إن كان `FACEBOOK_APP_SECRET` فارغاً (`log.warning(...); reject`) — عكس السلوك القديم "fail open". **تم الإصلاح.**
- ✅ **صلاحيات نقاط الدفع**: كل endpoint في `payments.py` (topup, confirm, create-subscription, admin-resolve) محمي بـ `Depends(get_current_user)` أو `require_role("admin")`. **لا يوجد** endpoint دفع مكشوف بدون مصادقة. **تم الإصلاح.**
- ✅ **CSRF**: يوجد middleware فعلي `csrf_origin_check` في `runner.py:418` يتحقق من Origin/Referer على الطلبات المُغيّرة للحالة. **موجود.**
- ⚠️ **[VERIFY] Rate limiting على `payments.py`**: لم أجد أي decorator/dependency لتحديد المعدل على أي endpoint في الملف (لا `rate_limit` ولا مشابه). يُحتاج تحقق وإصلاح صريح ضمن Track A.

### 1.8 حجم المشروع الفعلي (للسياق)

- الواجهة الأمامية: 108 ملف TS/TSX، 8,365 سطر
- راوترات الباك-إند: 35 ملف، 3,899 سطر
- 22 صفحة فرعية تحت `dashboard/` — منها 9 صفحات موصوفة سابقاً كـ"واجهة فقط قيد التطوير" في `delivery-report-smartbot-restoration.md` (يوليو): marketing, ads, reports, autoreply, notifications, tools, billing, support, leads. **[VERIFY]** — يجب على الوكيل التحقق من كل واحدة من هذه التسع تحديداً (وليس الافتراض) قبل البدء بالإصلاح، لأن بعضها قد يكون تغيّر في إعادة بناء سبتمبر (رصدتُ فعلياً أن `autoreply` و`tools` و`leads` و`billing` تحتوي الآن `fetch` واحد على الأقل — أي جزئياً متصلة، بعكس `notifications` و`support` المؤكد كونهما بلا أي اتصال).

---

## 2. رحلة العميل المطلوبة — تعريف "بدون مشاكل" بشكل قابل للاختبار

طلبك الأساسي: عميل يدخل → ينشئ حساباً → يشترك ويدفع → يصل لخدمات منظمة وجميلة، بلا تكرار ولا تشوه ولا تدخل يدوي. هذا التعريف التشغيلي (Definition of Done) الذي ستُقاس عليه كل الإصلاحات:

```
[زائر جديد] ──▶ /register ──▶ حساب منشأ + JWT صالح
      │
      ▼
[/pricing] ──▶ اختيار خطة ──▶ /subscribe?plan=X ──▶ اختيار مزوّد دفع
      │                                                     │
      │                                            USSD/تحويل + انتظار SSE
      ▼                                                     │
[/dashboard] ◀── تفعيل تلقائي فور موافقة الأدمن ◀───────────┘
      │
      ▼
23 قسم في الشريط الجانبي ── كلٌ يعمل بواجهة API حقيقية،
موحّدة الشكل، بدون بيانات وهمية، تعمل على الجوال وسطح المكتب
```

**معيار القبول النهائي لكامل الخطة:** تسجيل جديد → اشتراك → دفع → موافقة أدمن → دخول لوحة تحكم فعّالة بالكامل، **يُختبر ببرنامج E2E آلي واحد يمر بالخطوات الست كاملة ويُنتج تقرير PASS/FAIL بدليل من التيرمينال**، وليس بمراجعة بصرية فقط.

---

## 3. الخطة التنفيذية — 8 مسارات (Tracks)، كل مسار = مراحل بوكلاء متوازين

> **لماذا "مسارات" لا "مراحل" فقط كما في خطة يوليو؟** لأن نطاق طلبك (المظهر + الثيمات + التكرار + التشوه + الوظائف + الأدوات المفتوحة المصدر) أوسع من دفعة إصلاحات واحدة. كل Track مستقل قابل للتنفيذ بالتوازي مع غيره طالما لا تعارض في الملفات، لكن **Track A يجب أن يُنفَّذ ويُعتمد أولاً** لأنه يغيّر عقد البيانات الذي تعتمد عليه كل الواجهات.

### Track A — توحيد عقد الـ API (P0، حرج، 0 تبعيات) — 6 وكلاء

**الهدف:** كل الـ 35 راوتر يعيد `{"success": bool, "data": ..., "error"?: str}` بلا استثناء.

| # | المهمة | الوكلاء |
|---|---|---|
| A.1 | إنشاء `response_wrapper.py` مساعد مركزي (دالة `ok(data)` و`fail(error, code)`) في `fb_dashboard/_services.py` أو ملف جديد `fb_dashboard/_responses.py` | 1 |
| A.2 | تطبيق الغلاف على 15 راوتر (admin_routes, analytics, bot, facebook_routes, inbox, rules, scheduled_posts_routes, sequences, broadcasts, calendar_routes, crm_routes, diagnostics, flows, offers_routes, publisher_routes) | 4 (متوازين، كل وكيل ~4 ملفات) |
| A.3 | تطبيق الغلاف على 14 راوتر المتبقي (replies, reports_routes, subscribers_tags_routes, team_routes, templates_routes, users, webhooks, widgets_routes, brand_routes, alerts_routes, health_alerts_routes, commerce_routes + إكمال auth.py وai.py الجزئيين) | 3 |
| A.4 | تحديث **كل** استدعاء `fetch`/`apiFetch` في الواجهة الأمامية (108 ملف) ليتوقع `{success,data}` بشكل موحّد عبر دالة مساعدة واحدة `unwrapApi<T>()` في `src/lib/` بدل معالجة كل مكان بشكله الخاص | 2 |

**بوابة تحقق A (إلزامية قبل الانتقال لأي مسار آخر):**
```bash
# يجب أن تُرجع صفراً — أي راوتر لا يزال يستخدم dict خام
grep -L '"success":' fb_dashboard/routers/*.py | grep -v '.bak'
# تشغيل فعلي لكل endpoint (curl) والتحقق من شكل الاستجابة، وليس قراءة الكود فقط
pytest fb_dashboard/ -k "response_shape" -v
```

---

### Track B — رحلة العميل الحرجة: تسجيل → اشتراك → دفع → دخول (P0) — 5 وكلاء

| # | المهمة | ملاحظة الأدلة |
|---|---|---|
| B.1 | اختبار E2E حقيقي (Playwright، موجود `playwright: ^1.61.1` في devDependencies بالفعل) يمر بالمسار الكامل من القسم 2 على بيئة staging فعلية، ويُنتج تقرير screenshots + HAR لكل خطوة | استخدم `fb_dashboard/frontend/e2e/` الموجود مسبقاً — لا تنشئ بنية جديدة |
| B.2 | إصلاح أي انكسار يكتشفه B.1 فوراً (لا تخمين مسبق — القسم 1.7 أثبت أن أجزاء من خطة يوليو الأمنية أُصلحت فعلاً، فلا تعد إصلاحها) | — |
| B.3 | [VERIFY] rate limiting على `payments.py` (القسم 1.7) — أضف dependency تحديد معدل صريح على `topup`, `confirm`, `create-subscription` | — |
| B.4 | مراجعة `admin_routes.py` قسم "إدارة الاشتراكات" (شوهدت سابقاً بحقلي "المستخدم" و"الخطة" فارغين في نسخة قديمة من الواجهة) — تحقق إن كانت لا تزال فارغة بعد إعادة البناء، وإن كانت كذلك اربطها بـ `username`/`plan_name` الفعليين من `models.py` | [VERIFY] — اللقطة القديمة (يوليو) قد لا تعكس الكود الحالي |
| B.5 | التأكد أن الموافقة الإدارية على الدفع تُفعّل اشتراك المستخدم **فوراً** عبر SSE (لا انتظار polling بطيء)، وأن هذا مُختبر فعلياً لا مفترضاً | ابحث عن endpoint SSE الموجود فعلياً في الكود أولاً قبل افتراض غيابه |

**بوابة تحقق B:** تسجيل تشغيل Playwright الكامل يُظهر PASS من البداية للنهاية + لقطات شاشة لكل خطوة مرفقة بتقرير التسليم.

---

### Track C — تنظيف التكرار والكود الميت (شامل وليس عيّنة) — 4 وكلاء

القسم 1.2 وجد مثالين بفحص عيّنة فقط. هذا المسار يُلزم بمسح **كامل** المستودع:

| # | المهمة |
|---|---|
| C.1 | مسح آلي لكل ملفات `.tsx`/`.ts` بحثاً عن أزواج ملفات بنسبة تشابه >85% (سكربت بسيط بـ `difflib` في بايثون أو `jscpd` عبر npm) — وليس فقط الاعتماد على الاسم المتشابه |
| C.2 | مسح كل ملفات `.py` بنفس الطريقة في `fb_dashboard/` بالكامل (58 ملف)، وأي ملف `.bak`/`.old`/`_v2`/`_backup` في كامل المستودع (`find . -regex ".*\.\(bak\|old\|orig\)$"`) |
| C.3 | لكل تكرار مكتشف: تحديد أيهما "حي" (مستورد فعلياً) بـ grep، حذف الميت، وإن كان كلاهما حياً فدمجهما في مصدر واحد بحذر شديد (بدون كسر أي استيراد) |
| C.4 | حذف `payments.py.bak` تحديداً (مؤكد بالفعل) وإضافة `*.bak` إلى `.gitignore` لمنع تكرار المشكلة |

**بوابة تحقق C:** `jscpd` (أو أداة مماثلة) على كامل المستودع بعد التنظيف يُظهر نسبة تكرار أقل من عتبة محددة (مثلاً 3%)، مرفقة بتقرير قبل/بعد.

---

### Track D — نظام التصميم والثيمات (توحيد شامل، ليس ترقيعاً) — 5 وكلاء

القسم 1 أظهر أن نظام الـ tokens في `globals.css` (OKLCH، مصدر واحد للون البرتقالي `--accent`) **جيد فعلياً كأساس** — المشكلة ليست غياب نظام تصميم بل التأكد من التزام 100% من الشاشات به:

| # | المهمة |
|---|---|
| D.1 | معالجة الاستثناءين المؤكدين: `components/shared/Confetti.tsx` (14 لون hex مباشر) و`components/ui/iphone-mockup.tsx` (5 ألوان) — انقلها لمتغيرات CSS جديدة مخصصة (`--confetti-*`) بدل استخدامها الخام، حتى لو كانت الألوان نفسها مقبولة بصرياً (الاحتفال بالكونفيتي يحتاج تنوع ألوان طبيعياً) — المطلوب أن تكون معرّفة مركزياً لا مبعثرة |
| D.2 | مسح **كل** الشاشات الـ 22 في `dashboard/` + `admin/` بحثاً عن أي `bg-white`, `text-black`, أو أي كلاس Tailwind يكسر نظام `dark`/tokens (مثال حرج: لقطة شاشة قديمة أظهرت صفحة "إدارة الاشتراكات" بخلفية بيضاء فاتحة بينما لوحة التحكم الرئيسية داكنة تماماً — هذا **[VERIFY]** إن كان لا يزال قائماً في الكود الحالي بعد إعادة البناء، لأن اللقطة من يوليو) |
| D.3 | توحيد نمط الأيقونات: تأكد أن كل الأيقونات عبر المشروع من `lucide-react` حصراً (لا مزيج مع رموز نصية بديلة placeholder — لاحظت في لقطة تسعير قديمة عنصر أسئلة شائعة بأيقونة تبدو كنص بديل غير مرسوم، يحتاج **[VERIFY]** في الكود الحالي) |
| D.4 | فحص RTL شامل: كل حقول الإدخال (`input`) يجب أن تحمل `dir="auto"` (بند من خطة يوليو — تحقق إن نُفِّذ فعلاً الآن عبر `grep -L 'dir=' src/app/**/page.tsx`) |
| D.5 | توثيق نظام التصميم في `docs/design-system.md` جديد: كل token، متى يُستخدم كل لون (success/warning/info/destructive)، قواعد التباعد والـ radius — ليصبح مرجعاً يمنع أي وكيل مستقبلي من كسر الاتساق |

**بوابة تحقق D:** تشغيل سكربت فحص آلي (`grep -rn "bg-white\|bg-black\|text-\[#"`) على `src/app` و`src/components` يُرجع صفر نتائج خارج الاستثناءات الموثقة في `docs/design-system.md`.

---

### Track E — اكتمال الوظائف الحقيقي (لا واجهات فارغة) — 6 وكلاء

| # | المهمة |
|---|---|
| E.1 | **الإشعارات** (`notifications/page.tsx`): استبدال الـ `useState` المحلي بـ endpoint حقيقي جديد `/api/user/notification-preferences` (GET/PUT) + جدول DB (Alembic migration جديدة) + إزالة أو تصحيح جملة "تُحفظ تلقائياً" لتعكس الواقع فعلياً بعد الربط |
| E.2 | **الدعم** (`support/page.tsx`): استبدال بيانات التواصل الوهمية ببيانات حقيقية (اطلب من صاحب المشروع البريد/الهاتف الفعليين قبل النشر — **لا تخترع بيانات بديلة أخرى**)، أو اربطها بنظام تذاكر حقيقي إن وُجد |
| E.3 | لكل صفحة من التسع المذكورة في 1.8: تحقق فردي **[VERIFY]** لحالتها الفعلية الآن (متصلة بالكامل / جزئياً / لا شيء)، ثم أكمل الناقص بربط API حقيقي — لا حل جماعي واحد يصلح للكل |
| E.4 | إزالة سلسلة "هذه الصفحة قيد التطوير" (`[...slug]/page.tsx:99`) نهائياً بعد أن تصبح كل الأقسام الـ23 لها صفحات مخصصة فعلية — أو إن تعذّر لبعضها في هذه الدفعة، استبدال الرسالة بحالة "قريباً" احترافية مصممة (لا نص عادي فقط) مطابقة لنظام التصميم |
| E.5 | تفعيل `recharts` (القسم 1.4): استبدال كل رسم بياني يدوي بـ `<div style={height}>` في `dashboard/page.tsx` و`analytics/page.tsx` و`demo/page.tsx` بمكونات `recharts` حقيقية (LineChart/BarChart) متوافقة مع tokens الألوان من Track D | 
| E.6 | مراجعة `admin_routes.py` endpoint إدارة الاشتراكات المرتبط بـ B.4 — تأكيد نهائي بعد الإصلاح عبر بيانات حقيقية من قاعدة بيانات staging |

**بوابة تحقق E:** لكل صفحة من الـ22 — طلب `curl` فعلي للـ API الذي تعتمد عليه من بيئة staging يُظهر بيانات حقيقية (ليس `[]` فارغة دائماً ولا 404) + لقطة شاشة تُظهر بيانات فعلية معروضة.

---

### Track F — تجربة الجوال (إلزامية، القسم 1.5) — 3 وكلاء

| # | المهمة |
|---|---|
| F.1 | تصميم مكوّن `MobileBottomNav.tsx` أو `MobileDrawer.tsx` جديد (استخدم مهارة **frontend-design** المتاحة في بيئتك لضمان جودة بصرية، لا تصميماً عشوائياً) — إما شريط سفلي بأهم 5 أقسام + زر "المزيد" يفتح Sheet لباقي الـ18، أو Drawer كامل بزر Hamburger أعلى الصفحة |
| F.2 | ربطه في `DashboardShell.tsx` ضمن `md:hidden` (عكس الشريط الحالي `hidden md:block`) — استخدام نفس مصدر `NavSection`/`NavItem` من `AdminSidebar.tsx` لتفادي تكرار بيانات القائمة (راجع Track C) |
| F.3 | اختبار فعلي على viewport جوال (375px) عبر Playwright لكل صفحة من الـ22 — التأكد أن كل قسم قابل للوصول خلال ضغطتين كحد أقصى |

**بوابة تحقق F:** تسجيل فيديو/لقطات Playwright بعرض 375px يُظهر التنقل بين 5 أقسام مختلفة بنجاح.

---

### Track G — دمج الأدوات مفتوحة المصدر (مسار منفصل ومعزول) — 4 وكلاء

| # | المهمة |
|---|---|
| G.1 | إضافة `mcp` + الاعتماديات اللازمة إلى `requirements.txt` في **بيئة معزولة** (virtualenv/branch منفصل) — لا تلمس `fb_dashboard/fb_client.py` الحالي حتى يثبت التكامل الجديد |
| G.2 | استنساخ ودراسة `HagaiHen/facebook-mcp-server` كمرجع (وليس نسخاً حرفياً) — تكييف أدواته الـ~30 لتتوافق مع `models.py` ونمط tenant-isolation الحالي في SmartBot (المشروع متعدد المستأجرين — أي حل من مستودع خارجي يفترض صفحة واحدة يحتاج تكييفاً لدعم tenants متعددين) |
| G.3 | اختبار مقارن: تشغيل الأدوات الجديدة (عبر MCP) جنباً إلى جنب مع `fb_client.py` الحالي على بيئة staging فقط، ومقارنة النتائج قبل أي استبدال فعلي في الإنتاج |
| G.4 | استخدام مهارة **scroll-craft** (متاحة كـ Claude Code skill) حصراً على `app/page.tsx` (الهبوط) و`app/pricing/page.tsx` — وفق قواعدها الخاصة (نحو تفاعلي واحد فريد، لا تكرار نفس القالب المستخدم في مواقع أخرى) — **ممنوع استخدامها داخل `dashboard/`** |

**بوابة تحقق G:** تقرير مقارنة (staging فقط) بين الأداة القديمة والجديدة لكل دالة، بدون أي تغيير في الإنتاج حتى اعتماد صريح منك.

---

### Track H — تحقق شامل نهائي وتوثيق (بعد كل المسارات) — 3 وكلاء

| # | المهمة |
|---|---|
| H.1 | تشغيل Track B.1 (E2E الكامل) مرة أخيرة بعد كل الإصلاحات، من الصفر، على بيئة staging نظيفة |
| H.2 | تحديث `CLAUDE.md` بأي قاعدة هندسية جديدة نتجت عن هذه الخطة (مثال: "كل راوتر جديد يجب أن يستخدم `_responses.py`") حتى لا تتكرر الانحرافات مستقبلاً |
| H.3 | كتابة `docs/master-plan-2026-09-03-delivery-report.md` بنفس أسلوب `delivery-report-smartbot-restoration.md` الموجود — جدول لكل بند من كل Track: منفَّذ/لم ينفَّذ + دليل تيرمينال لكل بند، **وليس وصفاً نثرياً بدون إثبات** |

---

## 4. ترتيب التنفيذ والتبعيات

```
Track A (عقد API) ─── حرج، ينفَّذ أولاً ─────────────────┐
                                                          │
      ┌──────────────┬──────────────┬───────────────────┤
      ▼              ▼              ▼                    ▼
Track B          Track C         Track D              Track E
(رحلة العميل)    (تكرار)        (ثيمات)              (اكتمال وظائف)
      │              │              │                    │
      └──────────────┴──────────────┴────────────────────┘
                             │
                             ▼
                        Track F (جوال)
                             │
                             ▼
                    Track G (أدوات مفتوحة المصدر — معزول تماماً، يمكن أن يبدأ بالتوازي منذ البداية لأنه لا يلمس الكود الحي)
                             │
                             ▼
                        Track H (تحقق نهائي)
```

**إجمالي:** 8 مسارات، ~36 وكيلاً، بوابة تحقق إلزامية بعد كل مسار — لا انتقال دون إثبات تيرمينال.

---

## 5. قواعد إلزامية على كل وكيل ينفّذ هذه الخطة

هذه ليست اقتراحات — أي وكيل (Claude Code، أو أي بيئة تنفيذ آلية) يعمل على هذه الخطة **يجب** أن يلتزم بها حرفياً:

1. **ممنوع الاعتماد على الذكاء الذاتي فقط دون أدوات.** قبل أي تعديل على المظهر/الثيمات، استخدم مهارة **frontend-design** المتاحة إن وُجدت في بيئتك. قبل أي عمل على صفحة الهبوط أو التسعير، استخدم مهارة **scroll-craft** (Track G.4). قبل دمج قدرات فيسبوك جديدة، ارجع لمرجع `facebook-mcp-server` (Track G.2) بدل اختراع أدوات من الصفر.
2. **ممنوع الإصلاح بالتخمين.** كل بند مؤشَّر `[VERIFY]` في هذه الخطة يُفحص بأمر فعلي (`grep`/`curl`/تشغيل) قبل لمس الكود، والدليل يُرفق في تقرير التسليم.
3. **ممنوع الإبلاغ عن اكتمال دون دليل تيرمينال.** "تم الإصلاح" وحدها غير مقبولة — يُطلب ناتج أمر فعلي (نجاح اختبار، استجابة curl، لقطة شاشة) لكل بند في كل بوابة تحقق.
4. **تغييرات جراحية فقط.** لا تُعاد كتابة ملفات كاملة تعمل جزئياً؛ يُعدَّل ما يلزم بالضبط. أي إعادة هيكلة واسعة (مثل Track A) تُوثَّق بخطة تعديل ملف-بملف قبل التنفيذ.
5. **لا تكرار عمل سابق.** قبل البدء بأي بند، راجع القسم 1.7 (ما تم إصلاحه فعلاً) وتاريخ git (`git log`) للتأكد أن العمل لم يُنفَّذ مسبقاً.
6. **بوابات الاعتماد صارمة.** لا ينتقل أي مسار للمرحلة التالية دون اجتياز بوابة التحقق المكتوبة في قسمه أعلاه بدليل مرفق.

---

## 6. جاهز للتسليم لوكيل التنفيذ

هذا المستند مصمم ليُعطى مباشرة لـ Claude Code أو أي بيئة تنفيذ agentic تستخدمها (مثل `OpenDesign.ai` المذكورة في بيئتك)، مقسّماً كما هو أعلاه — كل Track يمكن أن يكون مهمة مستقلة تُعطى لوكيل أو مجموعة وكلاء متوازية، مع رجوع لهذا الملف كمرجع وحيد للحقيقة (Source of Truth) بدل الخطط المتعددة المتراكمة سابقاً (`docs/remediation-plan.md`, `docs/history/*`, `smartbot-incident-recovery-roadmap.md`) التي أصبحت جزئياً قديمة أو متداخلة.
