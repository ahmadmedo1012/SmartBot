alembic>=1.18.5
fastapi>=0.115.6
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.29.0
aiosqlite>=0.20.0
pydantic-settings>=2.0.0
jinja2>=3.1.0
python-multipart>=0.0.31
httpx>=0.28.0
bcrypt>=5.0.0
PyJWT>=2.13.0
python-dotenv>=1.0.0
openai>=1.0.0
# google-generativeai>=0.8.0  # optional — set GEMINI_API_KEY to use
tenacity>=9.1.4              # retry around LLM calls with backoff
jsonschema>=4.26.0           # validate agent JSON tool output before execution
Pillow>=12.3.0               # image resize/compress before base64 encode for vision
cryptography>=42.0.0
argon2-cffi>=23.1.0
redis[asyncio]>=5.4.0
sentry-sdk[fastapi]>=2.19.0        # v6 §C — error tracking (Sentry OR GlitchTip: same DSN wire protocol, no-op without SENTRY_DSN)

# v15-E9 (C-DEP1): the PDF guard test (tests/test_v14_engines.py::
# test_pdf_render_runs_off_event_loop) pins the v14-E2 fix — weasyprint's
# write_pdf() must run in a worker thread (asyncio.to_thread), never on the
# event loop. weasyprint was NOT in requirements.txt, so the guard was
# importorskip-disabled in CI and any environment installed from this file
# (D7-F3: cold alarm). The engine itself probes availability and falls back
# to fpdf2 when missing — but the guard needs the real library. The plan
# grants this as the one documented new dependency (v15 §3).
weasyprint>=60.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
aiosqlite>=0.20.0  # for SQLite in-memory test DB

# Track G (latest_plan.md) — STANDALONE MCP server ONLY.
# The live app does NOT import mcp at runtime; needed solely by
# fb_dashboard/facebook_engine/mcp_server.py when run as its own process:
#   python -m fb_dashboard.facebook_engine.mcp_server --tenant-id 1 ...
mcp>=1.2.0

# Code quality gates (v5 §2) — ruff lint + coverage measurement
# v15-E9 (C-RUFF1 / D9-L12): floor ruff>=0.6.0 was invalid — 0.6–0.8 crash on
# ruff.toml's UP045 selector ("Unknown rule selector"), and a floating floor
# let every CI run resolve a different ruff. Pin the 0.16 line (the tested
# version; 0.12/0.9 also parse the config but each flags slightly different
# sets — deterministic gate = pinned minor).
ruff>=0.16.0,<0.17
pytest-cov>=5.0.0
pygments>=2.20.0              # v15-E9 (OSV gate): transitive via mcp->rich; floor pinned so pip never resolves the vulnerable 2.9-2.15 line (ReDoS PYSEC-2023-117 + PYSEC-2026-2987)
