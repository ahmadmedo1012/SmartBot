# scripts/round.env — v16-E7 (D7-G6): الجولة الحالية — معلمة بدل التصلب.
# تقرأها scripts/gate_all.sh (source: بوابة الجاهزية [6] وخطة الجولة وعلامات
# الوثائق) و scripts/round_metrics.py (قيمة --round الافتراضية).
# غياب الملف أو فراغ القيمة = التراجع إلى v16 (نفس سقف هذه الجولة).
# عند فتح جولة جديدة: حدّث السطر التالي فقط.
ROUND=v16
