# خطة SmartBot v3 — الإطلاق النهائي العالمي (خطة إلزامية، تنفيذ متواصل حتى الكمال)

> **تاريخ الفحص:** 2026-09-05 (إعادة فحص بعد تنفيذ v2) | **آخر commit مفحوص:** `c8d018d8`
> **هذه الخطة إلزامية التنفيذ بالكامل، بلا توقف، بلا تبرير.** الوكيل يملك توكن GitHub وتوكن Vercel — **لا عذر لأي توقف قبل الوصول للنتيجة النهائية الموصوفة في القسم 7 (تعريف الاكتمال)**. كل بند غير منفَّذ أو غير مؤكَّد بالفحص المباشر (كود + متصفح حي بعد push للـ main) يُعتبر غير موجود، بغض النظر عن أي رسالة commit تدّعي عكس ذلك.

---

## 0. الحقيقة أولاً: ما تحقق فعلاً من v2 وما لم يتحقق (بالدليل لا بالثقة)

| البند | الادعاء (commit) | الفحص المباشر الآن | الحكم |
|---|---|---|---|
| توحيد دلالة `--accent`/`--primary` | `7718bc99` "unify --accent semantics with Smart-Menu" | `globals.css`: `--primary: oklch(0.55 0.19 45)` + `--accent: oklch(0.55 0.19 45 / 0.15)` — **مطابق حرفياً** لبنية Smart-Menu | ✅ **صحيح فعلاً** |
| الشعار (Header العام) | `2ee083fa` "brand icon 404 on every Vercel page — path now root-agreed" | `Header.tsx` يستخدم `<Image src="/brand-icon.png">` فعلياً في مكانين | ✅ **صحيح للصفحات العامة (الهبوط/تسجيل الدخول)** |
| **الشعار داخل لوحة التحكم نفسها** | (لم يُذكر صراحة كمُصلَح) | `AdminSidebar.tsx:98` **لا يزال**: `logo = "S"` — حرف نصي بسيط داخل مربع برتقالي، **ليس صورة** | ❌ **لم يُصلَح — هذا بالضبط ما تراه كل يوم داخل التطبيق وتشكو من غيابه** |
| الخطوط (تطابق Smart-Menu) | `d2f08026` "Smart-Menu font/identity parity" | `--font-heading` عند SmartBot: **Cairo أولاً** (`var(--font-cairo)...,"Readex Pro"`) — بينما عند Smart-Menu: **Readex Pro أولاً**. الترتيب معكوس، فكل العناوين تُعرض بخط مختلف عن Smart-Menu فعلياً رغم الادعاء | ❌ **لم يُصلَح فعلياً — فقط استُورد الخط دون تصحيح ترتيب الأولوية** |
| بيانات فيسبوك الحقيقية (رسائل/تعليقات) | `d2f08026` "messenger webhook ingestion + auto-reply, tenant-scoped FB data flow" | البنية التحتية **موجودة فعلياً وتبدو سليمة هندسياً**: `subscribe_page_webhooks()` يُستدعى عند الربط (`facebook_routes.py:117-121`)، `messenger_service.py` يحفظ الرسائل بعزل تام حسب `tenant_id` | ⚠️ **مبني بشكل صحيح في الكود، لكن غير مُتحقَّق منه حياً على الإطلاق** — لا يوجد أي دليل أن أحداً فتح متصفحاً وربط صفحة فيسبوك حقيقية وشاهد رسالة تصل فعلياً بعد النشر على Vercel |
| إشعارات تليجرام | `c9ccda8b` وتعليق داخل `telegram_bot.py` نفسه | الكود يعترف صراحة في تعليق أعلى الملف: *"BEFORE: BOT_TOKEN/ADMIN_IDS were env-only... the owner received ZERO telegram notifications"* — تم تعديله ليقرأ التوكن من قاعدة البيانات (`SystemConfig`) أولاً عبر `/admin/settings` مع احتياطي من متغيرات البيئة | ⚠️ **الكود مُصلَح، لكن هذا الإصلاح يتطلب منك أنت تحديداً إدخال توكن بوت تليجرام حقيقي ومعرّفات الأدمن الحقيقية من صفحة `/admin/settings` — إن لم تُدخلها بعد، ستستمر الإشعارات بالصفر رغم أن الكود سليم 100%. هذا فرق جوهري بين "خلل برمجي" و"إعداد ناقص" يجب التحقق منه أولاً قبل أي إصلاح إضافي في الكود** |

**الخلاصة الحاسمة لهذا القسم:** جزء كبير من شكواك الحالية جذره **إما (أ) مكوّن مرئي واحد لم يُلمس فعلياً رغم الادعاء (الشعار داخل اللوحة، ترتيب الخط) أو (ب) إعداد ناقص من طرفك يحتاج إدخال بيانات حقيقية (توكن تليجرام) أو (ج) كود مبني بشكل سليم نظرياً لكن لم يُختبر حياً بمتصفح فعلي منذ لحظة كتابته.** هذه الخطة تعالج الثلاثة معاً، بترتيب صارم.

---

## 1. المبدأ الحاكم لهذه الخطة: "لا يوجد إصلاح حتى يراه متصفح حي بعد الدفع للـ main"

كل الأخطاء المتبقية أعلاه لها قاسم مشترك: **إصلاحات ادُّعي أنها تمت بناءً على قراءة الكود فقط، دون تشغيل فعلي.** هذه الخطة تُلزم بدورة عمل واحدة لا يُحاد عنها لكل بند تنفيذي من الأقسام 3-6:

```
1) افحص الكود الحالي (grep/قراءة) — لا تخمين
2) نفّذ الإصلاح الجراحي (أصغر تغيير ممكن يحل المشكلة تحديداً)
3) git commit + git push origin main (التوكن متاح — لا تنتظر إذناً لكل push فردي طالما ضمن نطاق هذه الخطة)
4) انتظر اكتمال نشر Vercel فعلياً (استخدم توكن Vercel API للتحقق من حالة الـ deployment: READY وليس BUILDING/ERROR)
5) افتح المتصفح على رابط الإنتاج الفعلي (وليس localhost) وتحقق بصرياً/وظيفياً من نفس البند الذي أصلحته تحديداً
6) التقط لقطة شاشة من المتصفح الحي كدليل إثبات
7) فقط بعد نجاح الخطوة 6 → انتقل للبند التالي. فشل الخطوة 4 أو 5 يعني العودة للخطوة 2، وليس المتابعة
```

**ممنوع إغلاق أي بند بالاعتماد على قراءة الكود وحدها. القراءة تثبت النية، والمتصفح الحي وحده يثبت النتيجة.**

---

## 2. منهجية المقارنة الشاملة مع Smart Menu — "بالتفصيل الممل" كما طلبتَ حرفياً

المقارنات السابقة (v1، v2) كانت عيّنات موجّهة (توكن لون واحد، ملف دفع واحد). هذه المرة: **مسح كامل، عنصر بعنصر، بلا استثناء واحد.**

### 2.1 جرد كامل لكل صفحة في كلا المشروعين (وليس عينة)

الوكيل يُنشئ جدولاً في `docs/full-parity-audit.md` يضم **سطراً لكل صفحة** موجودة في أي من المشروعين (Smart-Menu له `src/app/(public)/*` و`src/app/(protected)/*`، SmartBot له `src/app/*` و`src/app/dashboard/*`)، بالأعمدة التالية لكل صف:

| اسم الصفحة | موجودة في Smart-Menu؟ | موجودة في SmartBot؟ | تطابق الألوان (لقطة شاشة) | تطابق الخطوط | تطابق التخطيط/التباعد | تطابق سلوك التمرير/الحركة | ملاحظات التداخل البصري إن وُجد |

**لا تكتفِ بالصفحات المتقابلة بالاسم** — إن وُجدت صفحة في Smart-Menu دون مقابل في SmartBot (أو العكس)، سجّلها أيضاً واقترح إن كانت يجب أن تُضاف.

### 2.2 جرد كامل للمكوّنات المشتركة (Component-by-Component)

لكل ملف في `src/components/` بكلا المشروعين (وليس فقط ما تشابه اسمه سابقاً):

1. قائمة كاملة بـ `find src/components -name "*.tsx"` في كلا المشروعين.
2. لكل مكوّن له نظير محتمل بالاسم أو الوظيفة (زر، بطاقة، جدول، نافذة منبثقة، شريط تنقل، رأس صفحة، تذييل...): افتح الاثنين جنباً إلى جنب وسجّل كل فرق في: نصف القطر (radius)، الظل (shadow)، التباعد الداخلي (padding)، حجم الخط، سمك الخط، لون الحدود، سلوك hover/focus/active، حركة الدخول (animation on mount).
3. أي مكوّن موجود في Smart-Menu وغير موجود إطلاقاً في SmartBot (مثال محتمل: مكوّنات تحريك/تمرير من scroll-craft، مكوّنات حالة فارغة "Empty State" مصممة، Toast/Snackbar مخصص) — **انقله بالمنهجية الإلزامية في القسم 2.4، لا تُعد اختراعه من الصفر.**

### 2.3 جرد كامل لتوكنات التصميم (ليس فقط الألوان الرئيسية)

افحص **كل متغير CSS** في كلا `globals.css` دون استثناء — الألوان الثانوية (success/warning/destructive/info)، الظلال (shadow-sm/md/lg)، أنصاف الأقطار، المسافات (spacing scale)، سرعة الانتقالات (transition durations/easing curves)، فلاتر الضبابية (backdrop-blur). أي فرق قيمة **أو دلالة** (كما اكتُشف سابقاً مع `--accent`) يُوثَّق فيه: القيمة عند كل مشروع + المعنى المقصود + القرار (توحيد لصالح أيهما ولماذا — Smart-Menu هو المرجع دائماً كما حددتَ).

### 2.4 قاعدة النقل الإلزامية (لا استثناء بعد الآن)

أي عنصر يُكتشف أنه أفضل/أنضج في Smart-Menu **يُنقل بمنطقه وتصميمه**، مع تكييف حتمي لطبقة البيانات (tenant/API) في SmartBot، متبوعاً فوراً بإعادة فحص شامل لكل ملف في SmartBot يستخدم نفس أسماء التوكنات/الأصناف التي جاءت مع العنصر المنقول (لأن هذا بالضبط ما سبّب عطل `--accent` في الجولة الماضية عندما نُقل جزئياً دون هذه الخطوة).

---

## 3. تتبّع وإصلاح كل تداخل بصري (Visual Overlap) — مسح منهجي لا انتظار للشكوى

"التداخلات" (عناصر تتراكب فوق بعضها، نص يخرج عن حدود صندوقه، أزرار تتراكب مع الشريط الجانبي على شاشات معينة) عادة أعراض لأحد ثلاثة أسباب جذرية — افحص الثلاثة على **كل صفحة من كل مشروع بكل حجم شاشة**:

1. **z-index غير منظم**: ابحث (`grep -rn "z-\[" --include="*.tsx"`) عن قيم z-index يدوية متفرقة بدل نظام طبقات موحّد (Modal > Dropdown > Sticky Header > Content). أنشئ `docs/z-index-scale.md` بمقياس رسمي واحد وطبّقه في كل مكان.
2. **مواضع `fixed`/`sticky` بدون حساب ارتفاع العناصر الأخرى**: أي عنصر `fixed bottom-0` (كشريط التنقل الجوال) يجب أن يُضاف له `padding-bottom` مكافئ في المحتوى الذي تحته، على **كل صفحة** بلا استثناء — تحقق يدوياً لا افتراضاً.
3. **حاويات بدون `overflow` محدد** تسمح بخروج المحتوى الطويل (أسماء عملاء طويلة، نصوص عربية RTL طويلة) خارج حدود البطاقة.

**التحقق الإلزامي:** افتح كل صفحة من الـ22+ صفحة بالمتصفح فعلياً على 3 أحجام شاشة (375px جوال، 768px تابلت، 1440px سطح مكتب) والتقط لقطة لكل مجموعة — أي تراكب أو خروج عن الحدود يُسجَّل ويُصلح فوراً قبل الانتقال للصفحة التالية.

---

## 4. التحقق الحي الكامل من منطق البوت (لا افتراض أن الكود "يجب أن يعمل")

القسم 0 أثبت أن البنية التحتية (webhook، اشتراك الصفحة، عزل المستأجرين) مبنية بشكل سليم نظرياً. **الآن يُطلب إثبات تشغيلي فعلي**، بترتيب:

1. **[VERIFY حي]** افتح `/admin/settings` بمتصفح فعلي على رابط الإنتاج، وتأكد أن حقل "Facebook App Secret" وحقل "Telegram Bot Token" وحقل "Telegram Admin IDs" إما معبّأة ببيانات حقيقية فعلاً، أو أبرزها بوضوح شديد في واجهة المستخدم (تنبيه أحمر بارز أعلى لوحة التحكم: "لم يتم إعداد بوت تليجرام — لن تصلك إشعارات") بدل الصمت الحالي الذي يجعل غياب الإشعارات يبدو كخلل خفي غامض.
2. **[VERIFY حي]** اربط صفحة فيسبوك اختبارية حقيقية (أو استخدم بيانات اختبار Facebook الرسمية إن كان توفرها ممكناً)، أرسل رسالة حقيقية لها، وتأكد بصرياً في `/dashboard/inbox` أنها تصل فعلاً خلال ثوانٍ (وليس صفراً كما تشتكي الآن).
3. **[VERIFY حي]** تأكد أن الرد الآلي (auto-reply / `rules.py` / bot engine) يُفعَّل فعلياً على الرسالة المُرسلة في الخطوة 2 — راقب سجل الخادم (server logs عبر Vercel/الاستضافة) في نفس اللحظة للتأكد من دخول الطلب فعلاً لمسار `_process_webhook_messaging` ثم `get_bot_engine`.
4. **[VERIFY حي]** كرّر نفس منطق التحقق (اتصال حقيقي → بيانات حقيقية تظهر خلال ثوانٍ) لكل قسم يشتكي المستخدم من ظهوره فارغاً/صفرياً: الإعلانات (ads)، التقارير (reports)، الحملات — **لا تكتفِ بإصلاح الرسائل واعتبار البقية "نفس المنطق تلقائياً"**؛ لكل قسم مسار API مختلف يجب التحقق منه فردياً بنفس الأسلوب.
5. **[VERIFY حي]** أنشئ اشتراكاً تجريبياً كاملاً (تسجيل → اختيار خطة → دفع تجريبي) وتأكد أن إشعار "اشتراك جديد" يصل فعلياً لتليجرام الأدمن بعد إعداد الخطوة 1 بشكل صحيح — وليس فقط أن الكود "يستدعي الدالة".

---

## 5. الشعار والهوية البصرية — إصلاح جذري لا ترقيعي

1. استبدل `logo = "S"` في `AdminSidebar.tsx` (والـ`MobileBottomNav.tsx` إن كان به نفس النمط) بمكوّن `<Image src="/brand-icon.png" ... />` حقيقي، بنفس الطريقة المستخدمة بالفعل في `Header.tsx` — **هذا نفس الحل الموجود مسبقاً في المشروع، فقط لم يُطبَّق داخل لوحة التحكم**، فلا حاجة لتصميم جديد، فقط توحيد الاستخدام.
2. افحص **كل مكان آخر** في المشروع (صفحة تسجيل الدخول، بريد إلكتروني تلقائي إن وُجد، صفحة الخطأ 404، حالة التحميل الأولى قبل جلب البيانات) بحثاً عن نفس نمط "حرف نصي بديل عن الشعار" وطبّق نفس الإصلاح في كل موضع.
3. تحقق أن نفس الشعار يظهر في تبويب المتصفح (favicon) — قارن `favicon.png` الحالي مع نظيره في Smart-Menu للتأكد من التطابق في الدقة ونمط التصميم لا فقط الوجود.
4. **بوابة تحقق:** لقطة شاشة حية من رابط الإنتاج تُظهر الشعار الفعلي (لا حرف "S") في: الشريط الجانبي لسطح المكتب، شريط التنقل السفلي للجوال، تبويب المتصفح.

---

## 6. الخطوط — إصلاح ترتيب الأولوية (ليس استيراد خط جديد)

المشكلة ليست غياب Readex Pro (هو مستورد فعلاً) بل ترتيبه بعد Cairo في سلسلة `--font-heading`. أصلح الترتيب في كلا الوضعين (الداكن والفاتح) في `globals.css` ليطابق Smart-Menu حرفياً: `"Readex Pro"` أولاً ثم `var(--font-cairo)` احتياطياً. تحقق حياً بلقطة شاشة لعنوان رئيسي (`h1`) في نفس الصفحة على كلا المشروعين جنباً إلى جنب — يجب أن يتطابق شكل الحروف تماماً.

---

## 7. تعريف "الاكتمال" — لا يُعتبر أي شيء منجزاً قبل تحقق كل هذه الشروط معاً

هذا القسم هو معيار القبول الوحيد. الوكيل **لا يتوقف ولا يُبلغ عن انتهاء المهمة** حتى تتحقق كل السطور التالية، مثبتة بلقطة شاشة من رابط الإنتاج الحي بعد push فعلي لـ main:

- [ ] الشعار الحقيقي (لا حرف نصي) ظاهر في كل موضع بالتطبيق: اللوحة، الجوال، تبويب المتصفح، الصفحات العامة
- [ ] كل عنوان (h1/h2/h3) في SmartBot يستخدم بصرياً نفس خط Readex Pro الظاهر في Smart-Menu، مؤكَّد بلقطة مقارنة
- [ ] لا يوجد أي تراكب/خروج عن الحدود بصرياً في أي صفحة على 3 أحجام شاشة (375/768/1440)
- [ ] كل توكن لون في `globals.css` مطابق لـ Smart-Menu قيمةً ومعنىً، موثَّق في `docs/design-system.md` المحدَّث
- [ ] رسالة فيسبوك حقيقية تصل لصندوق الوارد خلال ثوانٍ من إرسالها فعلياً (مُختبر حياً لا افتراضاً)
- [ ] الرد الآلي يعمل فعلياً على رسالة حقيقية واردة (مؤكَّد من سجلات الخادم في نفس لحظة الاختبار)
- [ ] أقسام الإعلانات/التقارير/الحملات تعرض بيانات حقيقية غير صفرية بعد ربط حساب حقيقي (كل قسم مُختبر فردياً)
- [ ] إشعار تليجرام لدفعة/اشتراك تجريبي يصل فعلياً لمحادثة تليجرام حقيقية بعد إدخال توكن حقيقي في `/admin/settings`
- [ ] كل صفحة في جدول `docs/full-parity-audit.md` (القسم 2.1) محدَّثة بحالة "مطابق" وليس "قيد المراجعة"
- [ ] رحلة العميل الكاملة (تسجيل → اشتراك → دفع → موافقة → دخول لوحة عاملة بالكامل) منفَّذة ببرنامج E2E حي على رابط الإنتاج نفسه، لا على بيئة تطوير محلية

**فقط عند تحقق كل نقطة أعلاه بدليل — تُكتب `docs/v3-final-launch-report.md` وتُعتبر المهمة منتهية. أي نقطة غير محققة تعني الاستمرار في العمل دون توقف، مهما استغرق ذلك من ساعات.**
