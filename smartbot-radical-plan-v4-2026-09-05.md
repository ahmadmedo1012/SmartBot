# خطة SmartBot v4 — الإصلاح الجذري البنيوي + تعبئة عدد كبير من الوكلاء

> **تاريخ الفحص:** 2026-09-05 (إعادة فحص بعد v3) | **آخر commit مفحوص:** `9d9373ec`
> **هذه الخطة جذرية لا ترقيعية.** كل الجولات السابقة (v1، v2، v3) أصلحت أعراضاً فردية (لون، خط، ملف) فظهرت أعراض جديدة بنفس الجذر. هذه المرة **الجذر نفسه** هو الهدف، مع تنفيذ فعلي مُثبت — لا أوامر عامة، بل ملفات وأسطر وأسماء دوال محددة بالضبط.

---

## 0. التشخيص الجذري: لماذا تتكرر نفس المشكلة رغم 3 جولات إصلاح؟

فحصتُ الكود مباشرة بعد ادعاء v3 اكتمال بنود الخطوط والألوان، ووجدت **نمطاً هندسياً واحداً متكرراً** هو السبب الحقيقي وراء كل شكواك المتلاحقة عن "عدم تناسق" و"تداخل" و"ألوان مختلفة":

### 0.1 الدليل القاطع: تعريفان متضاربان لنفس المتغير في نفس الملف

`fb_dashboard/frontend/src/app/globals.css`:
- **السطر 31** (داخل قسم الخطوط الأول): `--font-heading: "Readex Pro", var(--font-cairo), ...` ← **صحيح، مطابق لسمارت مينيو**
- **السطر 109** (داخل قسم منفصل تماماً عنوانه "ACCENT — SINGLE SOURCE OF TRUTH"، أي قسم لا علاقة له بالخطوط أصلاً): `--font-heading: var(--font-cairo), "Cairo", "Readex Pro", ...` ← **معكوس، يبطل الإصلاح الأول تماماً لأن CSS يأخذ آخر تعريف يظهر في نفس النطاق**

هذا يعني: عندما "أصلح" وكيل سابق ترتيب الخط في v3، **كان يعدّل مكاناً واحداً فقط ظاناً أنه المصدر الوحيد**، بينما يوجد تعريف ثانٍ لاحق له في الملف نفسه يبطل التعديل بصمت. هذا **بالضبط** ما يفسر شعورك بأن الإصلاحات "لا تصل للنتيجة النهائية" رغم أن كل جولة تدّعي الإصلاح.

### 0.2 نفس النمط يتكرر في الألوان: ثلاثة أسماء لنفس القيمة

```
--accent: oklch(0.55 0.19 45 / 0.15)   ← المصدر "الرسمي" حسب تعليق الكود نفسه
--primary: oklch(0.55 0.19 45)          ← نفس اللون، اسم مختلف
--orange: oklch(0.55 0.19 45)           ← نفس اللون بالضبط، معرَّف بشكل مستقل تماماً (مرتين: سطر 170 للداكن، سطر 258 للفاتح)
```

**54 ملف** في المشروع لا يزال يستخدم `text-orange`/`bg-orange`/`border-orange` (الاسم الثالث القديم) بدل `text-accent-fg`/`bg-primary` (الاسم الرسمي حسب تعليق الكود نفسه الذي يقول حرفياً: *"Every other orange/primary/ring variable MUST be derived from them, NEVER set independently"*). طالما بقيت ثلاثة أسماء حية لنفس المفهوم، **أي تعديل مستقبلي على واحد فقط سيخلق تبايناً جديداً فوراً** — هذا هو مصنع الأعطال المستمر.

### 0.3 نفس النمط في لوحة التحكم التجريبية (Demo) — تفسير دقيق لشكواك عنها تحديداً

فحصت `app/demo/page.tsx` (247 سطر) وهذا ما وجدته حرفياً:

1. **أزرار التبويبات وهمية بصرياً بالكامل** — الكود (سطر 76-85) يرسم كل عنصر تنقل كـ`<div>` عادي بصنف `cursor-pointer` (يوحي بصرياً أنه قابل للنقر) **لكن بلا `onClick` إطلاقاً**. العنصر الوحيد الذي يبدو "نشطاً" (`لوحة البيانات`) مُثبَّت يدوياً `active: true` بشكل دائم في الكود نفسه (سطر 40) ولا توجد أي حالة (`useState`) تُغيّر أياً منها. **هذا تفسير حرفي 100% لشكواك: "لا يمكن التحكم فيها أو التنقل بين التبويبات" — لأنها فعلياً غير متصلة بأي منطق تنقّل، مجرد رسم ثابت.**
2. **الشريط الجانبي والهيدر في صفحة Demo مبنيان من الصفر داخل نفس الملف** (سطر 64-114) بدل استخدام مكوّني `DashboardShell.tsx`/`AdminSidebar.tsx` الحقيقيين المستخدَمين في كل صفحات لوحة التحكم الفعلية. **هذا تفسير حرفي لشكواك: "لا علاقة لها بلوحة التحكم الحقيقية"** — فعلياً هي شجرة مكوّنات مختلفة تماماً، مصادفة أنها تستخدم بعض بطاقات UI المشتركة (`Card`, `Button`) لكن الهيكل العام (تخطيط، تنقل، حالة نشطة) معاد اختراعه بمعزل تام.
3. تستخدم `text-orange`/`bg-orange` (الاسم القديم من 0.2) بدل التوكن الرسمي — فتظهر بلون مطابق للقيمة لكن **غير متسقة بنيوياً** مع بقية اللوحة، وأي تغيير مستقبلي في اللون الرسمي لن ينعكس عليها.

**الخلاصة الجذرية:** المشكلة ليست "تحتاج تحسين ألوان/خطوط/تباعد" كطلب عام — المشكلة أن **المشروع يحتوي عدة نسخ متوازية غير متصلة من نفس المفاهيم** (تعريفا خط، ثلاثة أسماء لون، شجرة UI منفصلة كاملة لصفحة Demo). أي إصلاح جزئي مستقبلي سيتكرر فشله ما لم تُحذف النسخ الزائدة نهائياً لا أن تُعدَّل فقط.

---

## 1. القرار الجذري (لا نقاش، لا خيارات بديلة)

1. **يُحذف تعريف `--font-heading` المكرر (السطر 109 وما حوله) نهائياً من الملف.** يبقى تعريف واحد فقط لكل متغير خط في كامل `globals.css` — إن وُجد أي متغير آخر معرَّف أكثر من مرة (`grep -c` لكل اسم متغير، أي عدد أكبر من 1 لكل وضع/theme يعني تكراراً يجب حذفه)، يُطبَّق نفس المبدأ عليه بلا استثناء.
2. **يُحذف `--orange`/`--orange-foreground`/`--orange-muted`/`--gradient-orange` نهائياً** من `globals.css` و`tailwind` theme mapping. تُستبدل كل الاستخدامات الـ54 في الملفات بـ`--accent`/`--accent-fg`/`--primary` بحسب المعنى الدقيق في كل موضع (وليس استبدالاً نصياً أعمى — راجع القسم 3 للمنهجية). الاستثناء الوحيد المسموح: `--confetti-orange` (يخدم غرضاً مختلفاً تماماً — الاحتفال البصري — ويبقى كما هو).
3. **صفحة `app/demo/page.tsx` تُعاد كتابتها بالكامل من الصفر باستخدام المكوّنات الحقيقية** (`DashboardShell`, `AdminSidebar`) بدل الشجرة المستقلة، مع تنقّل تبويبات حقيقي فعلي (`useState<TabKey>` + عرض محتوى مختلف فعلياً لكل تبويب، ولو كانت بيانات العرض التجريبي (`mockStats`) نفسها تبقى وهمية بوضوح كما هي الآن عبر شارة "تجربة - بيانات وهمية" الموجودة أصلاً — **البيانات الوهمية للعرض التجريبي مقبولة ومقصودة، لكن التنقل والهيكل يجب أن يكونا حقيقيين طبق الأصل من اللوحة الفعلية**).

---

## 2. تعبئة الوكلاء — 7 فرق متخصصة، ملفات محددة لكل فريق بلا تداخل

**قاعدة صارمة لمنع تعارض العمل المتوازي:** كل فريق يملك قائمة ملفات محددة أدناه ولا يلمس أي ملف خارجها. أي حاجة لتعديل ملف مملوك لفريق آخر تُنسَّق عبر ملف `docs/v4-coordination-log.md` (سطر واحد لكل تنسيق: من، إلى، الملف، السبب) قبل التنفيذ.

### الفريق 1 — توحيد التوكنات الجذري (2 وكيل)
**ملفات الملكية:** `globals.css` فقط، بالإضافة لأي ملف `tailwind.config.*`
- 1.1: احذف كل تعريف مكرر لأي متغير CSS في الملف (استخدم `grep -c "^\s*--VARNAME:" globals.css` لكل متغير موجود، أي نتيجة > 2 لكل ملف (واحد للداكن وواحد للفاتح) = تكرار يجب حذفه). وثّق كل حذف في `docs/v4-coordination-log.md`.
- 1.2: احذف `--orange`/`--orange-foreground`/`--orange-muted`/`--gradient-orange` (باستثناء `--confetti-orange`).
- 1.3: أنشئ سكريبت تحقق آلي `scripts/check-css-token-duplication.py` يفشل (exit code 1) إن وُجد أي متغير CSS معرَّف أكثر من مرتين في `globals.css` (مرة لكل وضع) — يُضاف كخطوة في CI لمنع تكرار هذا العطل مستقبلاً نهائياً.

### الفريق 2 — ترحيل الملفات الـ54 من `orange` للتوكن الرسمي (4 وكلاء، كل وكيل ~13-14 ملف)
**ملفات الملكية:** الـ54 ملف الناتجة عن `grep -rl "text-orange\b\|bg-orange\b\|border-orange\b" src --include="*.tsx"` (نفّذ الأمر بنفسك أولاً للحصول على القائمة الفعلية الحالية، القائمة أعلاه قد تغيرت بعد عمل الفريق 1)
- لكل ملف: افتحه، افهم **المعنى المقصود** من كل استخدام (لون نص رئيسي؟ خلفية زر؟ حدّ تفاعلي؟) ثم استبدله بالتوكن الدلالي الصحيح (`text-accent-fg`, `bg-primary`, `border-primary`, إلخ — حسب القاموس الموحّد من الفريق 1)، **وليس استبدالاً نصياً أعمى بأمر sed/find-replace واحد لكل الملفات** — لأن هذا بالضبط أسلوب الترقيع الذي فشل 3 مرات سابقاً.
- بعد كل ملف: التقط لقطة شاشة من رابط الإنتاج الحي لنفس الصفحة قبل وبعد التعديل، وتأكد أن اللون **مطابق بصرياً 100%** لما كان عليه (لأن القيمة العددية للون لم تتغير، فقط اسم المتغير — أي تغيير بصري ملحوظ يعني خطأ في الاستبدال).

### الفريق 3 — إعادة بناء صفحة Demo من الصفر (2 وكيل)
**ملفات الملكية:** `app/demo/*` فقط
- 3.1: احذف الشجرة المستقلة الحالية (سطور 64-114 من `page.tsx`) بالكامل.
- 3.2: أعد بناء الصفحة لتستخدم `<DashboardShell>` الحقيقي (`import { DashboardShell } from "@/app/dashboard/DashboardShell"` أو المسار الصحيح الحالي — تحقق منه أولاً) بنفس `AdminSidebar` الحقيقي، مع تمرير خاصية أو سياق (`context`/`prop`) يُفعّل "وضع العرض التجريبي" (شارة "تجربة - بيانات وهمية" تبقى ظاهرة، وزر "ابدأ الاشتراك" يبقى كما هو) دون إعادة اختراع الهيكل.
- 3.3: نفّذ تنقّلاً حقيقياً بين التبويبات: `useState<"stats"|"replies"|"audience"|"activity"|"analytics"|"schedule"|"settings">("stats")`، وكل تبويب يعرض محتوى `mockStats` مختلفاً فعلياً (البيانات تبقى وهمية، لكن كل تبويب يريك شيئاً مختلفاً حقيقياً عند النقر — وليس شاشة ثابتة).
- 3.4: تأكد أن الألوان تستخدم التوكن الرسمي من الفريق 1 حصراً (لا `text-orange` متبقٍ في هذا الملف).

### الفريق 4 — الأيقونات والمؤثرات الحركية (3 وكلاء)
**ملفات الملكية:** كل ملف تحت `components/ui/`, `components/shared/`, بالإضافة لأي استخدام `lucide-react`/`framer-motion` عبر `src/app/dashboard/**`
- 4.1: جرد كامل لكل أيقونة مستخدمة في المشروعين (SmartBot و Smart-Menu) — أي أيقونة تُستخدم في Smart-Menu لمفهوم معين (مثال: أيقونة "الإشعارات"، أيقونة "الإعدادات") ويُستخدم لها أيقونة **مختلفة** في SmartBot لنفس المفهوم، وحّدها لتطابق اختيار Smart-Menu حرفياً (نفس اسم الأيقونة من `lucide-react`).
- 4.2: جرد كامل لكل حركة/مؤثر دخول (`framer-motion` variants) في المشروعين — تأكد أن مدة الحركة (`duration`)، منحنى التسارع (`ease`)، ومسافة الحركة (`y`/`x` offset في `fadeUp` ونحوه) متطابقة بين `lib/motion.ts` (أو الملف المكافئ) في كلا المشروعين. أي فرق قيمة يُوحَّد لصالح Smart-Menu.
- 4.3: تأكد أن كل أيقونة داخل دائرة/مربع ملوّن (نمط شائع في بطاقات الإحصائيات) تستخدم نفس نسبة الشفافية للخلفية (`/10` أو ما يعادلها) في كل مكان — فحصت مثالاً في `demo/page.tsx:130` يستخدم `s.color.replace("text", "bg") + "/10"` بطريقة برمجية هشة (تستبدل نص "text" داخل اسم الصنف الكامل) قد تكسر مع أي تغيير مستقبلي في تسمية الألوان — استبدلها بخريطة صريحة `{color: string, bgClass: string}` بدل الاستبدال النصي الهش.

### الفريق 5 — مسح شامل للتداخلات البصرية على كل الشاشات (3 وكلاء)
**ملفات الملكية:** لا ملكية حصرية — فريق فحص واختبار فقط، يُبلّغ الفرق الأخرى بالمشاكل عبر `docs/v4-coordination-log.md` ولا يعدّل الكود مباشرة إلا بعد التنسيق
- افتح **كل صفحة** (22+ صفحة لوحة تحكم + الصفحات العامة) على 375px/768px/1440px بمتصفح فعلي على رابط الإنتاج، والتقط لقطة لكل توليفة. أي تراكب/خروج عن الحدود/تزاحم عناصر يُسجَّل في `docs/full-parity-audit.md` (الملف الذي أنشأته الخطة السابقة — إن لم يكن موجوداً أو غير مكتمل، أكمله الآن بالكامل، لا تنشئ ملفاً جديداً موازياً).

### الفريق 6 — مطابقة نهائية شاملة مع Smart-Menu (2 وكيل)
**ملفات الملكية:** لا ملكية حصرية — مراجعة نهائية بعد انتهاء الفرق 1-5
- كرر بالضبط منهجية القسم 2 من خطة v3 (جرد صفحة بصفحة، مكوّن بمكوّن) لكن هذه المرة **بعد** إصلاحات الفرق 1-5، للتأكد أن التوحيد الجذري لم يفتح فجوات جديدة، وأن كل صفحة أُغلقت فعلياً في `docs/full-parity-audit.md` بحالة "مطابق" لا "قيد المراجعة".

### الفريق 7 — الحوكمة والتحقق النهائي (1-2 وكيل)
**ملفات الملكية:** `docs/v4-*`, سكريبتات CI
- يجمع تقارير كل الفرق، يُشغّل `scripts/check-css-token-duplication.py` (من الفريق 1) للتأكد من نجاحه، يُشغّل اختبار E2E الكامل من الخطط السابقة على رابط الإنتاج بعد كل التعديلات، ويكتب `docs/v4-final-report.md` بجدول: كل بند من هذه الخطة → دليل تيرمينال/لقطة شاشة → PASS/FAIL. **لا يُعلن اكتمال العمل قبل أن يكون كل سطر PASS.**

---

## 3. منهجية إلزامية لكل ترحيل توكن (يُطبّق حرفياً في الفريق 2)

هذه هي بالضبط الخطوة التي غابت في كل محاولة سابقة وسببت التكرار:

```
لكل ملف فيه استخدام للتوكن القديم:
  1. اقرأ السياق البصري الفعلي (ما الذي يُلوَّن؟ نص؟ خلفية؟ حدّ؟ عند أي حالة: عادي/hover/active؟)
  2. حدد التوكن الدلالي الصحيح من القاموس الموحّد (وليس التخمين)
  3. عدّل الملف
  4. Push إلى main (Vercel token متاح)
  5. انتظر نجاح النشر فعلياً (Vercel API)
  6. افتح الصفحة في متصفح حي، قارن لقطة شاشة قبل/بعد
  7. الفرق البصري يجب أن يكون صفراً (نفس القيمة اللونية، فقط الاسم البرمجي تغيّر)
  8. فرق بصري ملحوظ = خطأ في الخطوة 2، عد للخطوة 1
```

**ممنوع تجميع كل الـ54 ملف في أمر استبدال نصي واحد ثم الانتقال للملف التالي دون تحقق فردي — هذا بالضبط ما أنتج التراكمات الحالية.**

---

## 4. المهارات والمشاريع مفتوحة المصدر الإلزامية في هذه الجولة

1. **مهارة `frontend-design`** (متاحة في بيئة التنفيذ) — إلزامية الاستخدام في الفريق 3 (إعادة بناء Demo) والفريق 4 (الأيقونات/الحركة) — لا اجتهاد بصري حر دون الرجوع لها أولاً.
2. **أداة اختبار انحدار بصري مفتوحة المصدر** — لمنع تكرار "الادعاء بالإصلاح دون إثبات" الذي حدث 3 مرات: أضف [`pixelmatch`](https://github.com/mapbox/pixelmatch) (مكتبة JS خفيفة، لا تتطلب خدمة سحابية) إلى خط أنابيب اختبارات Playwright الموجود مسبقاً في `e2e/` — بحيث تُقارَن لقطة شاشة كل صفحة قبل/بعد أي commit آلياً، ويفشل الاختبار إن تجاوز الفرق البصري عتبة محددة (مثلاً 1%) لأي صفحة **لم تكن مقصودة بالتعديل**. هذا يحوّل "التحقق بالمتصفح" من خطوة يدوية قد تُنسى إلى بوابة CI حتمية.
3. راجع مستودع Smart-Menu كما في الخطط السابقة — لا تكتفِ بالمقارنة اليدوية هذه المرة، بل استخدم نفس أداة `pixelmatch` لمقارنة لقطات الصفحات المتناظرة بين المشروعين آلياً حيث أمكن (نفس viewport، نفس المسار المنطقي).

---

## 5. الأمر الحاسم: لا توقف قبل هذا الجدول بالكامل PASS

| # | البند | معيار PASS |
|---|---|---|
| 1 | لا يوجد أي متغير CSS معرَّف أكثر من مرة واحدة لكل وضع في `globals.css` | `scripts/check-css-token-duplication.py` يخرج بـ exit code 0 |
| 2 | لا وجود لـ `--orange`/`--orange-foreground`/`--orange-muted` في `globals.css` أو `tailwind.config` | `grep -c` = 0 |
| 3 | صفر ملفات تستخدم `text-orange\|bg-orange\|border-orange` | `grep -rl` يعيد قائمة فارغة |
| 4 | صفحة Demo تستخدم `DashboardShell`/`AdminSidebar` الحقيقيين | مراجعة كود + لقطة شاشة تُظهر نفس هيكل اللوحة الحقيقية |
| 5 | كل تبويب في صفحة Demo قابل للنقر ويغيّر المحتوى فعلياً | فيديو/تسلسل لقطات Playwright يُظهر تبديل 3 تبويبات على الأقل |
| 6 | صفر تراكب بصري على 375/768/1440px في كل صفحة | جدول `docs/full-parity-audit.md` مكتمل 100% بحالة "مطابق" |
| 7 | مطابقة الأيقونات والحركات مع Smart-Menu موثّقة | تقرير الفريق 4 بجدول قبل/بعد |
| 8 | اختبار الانحدار البصري (`pixelmatch`) مدمج في CI ويعمل | تشغيل فعلي ناجح على الأقل مرة واحدة، موثّق في `docs/v4-final-report.md` |
| 9 | E2E الكامل لرحلة العميل ناجح على رابط الإنتاج بعد كل التعديلات | تقرير تشغيل مرفق |

**العمل يستمر بلا توقف، بعدد الوكلاء الموصوف أعلاه، بالتوازي وفق ملكية الملفات المحددة، حتى تتحقق كل نقطة من الجدول أعلاه بدليل فعلي — لا بادعاء نصي في رسالة commit.**
