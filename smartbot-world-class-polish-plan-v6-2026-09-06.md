# خطة SmartBot v6 — من "يعمل بشكل صحيح" إلى "مستوى عالمي" (أداء + إتاحة + موثوقية + دقة محتوى)

> **تاريخ الفحص:** 2026-09-06 | **آخر commit مفحوص:** أحدث نسخة بعد `docs/v4-final-report.md`
> **سياق مهم:** تحققتُ مباشرة أن v4 نُفِّذ فعلاً وبدقة عالية (تعريف خط واحد لا اثنان، صفر `--orange`، ديمو بشريط حقيقي وتبويبات تعمل 6/6، pixelmatch يعمل، 266 اختبار pytest ناجح). **لن أكرر التحقق من هذه البنود.** هذه الخطة تنتقل لطبقة جديدة تماماً: المشروع الآن "يعمل بشكل صحيح بنيوياً"، والمطلوب الآن هو الفرق بين ذلك وبين "مستوى عالمي" — وهو فرق يُقاس في تفاصيل لا تظهر إلا بأدوات قياس متخصصة (أداء، إتاحة، موثوقية تشغيلية)، لا بالفحص البصري وحده.

---

## 0. تنبيه حرج يخصّك أنت شخصياً — اقرأه قبل أي شيء آخر

تقرير الوكيل نفسه (`docs/v4-final-report.md`، القسم الأخير) ينص حرفياً: *"إبطال كل التوكنات (GitHub PAT + Vercel) وتغيير كلمة المرور بعد الانتهاء"*. هذا ليس بنداً تقنياً في الخطة — **هذا تحذير موجَّه إليك مباشرة من الوكيل نفسه** لأنه يملك حالياً صلاحية كاملة على مستودعك وحساب Vercel الخاص بك. طالما هذه الخطة (v6) قيد التنفيذ، أبقِ التوكنات فعالة، **لكن بعد اكتمال v6 وقبول النتيجة النهائية، نفّذ هذا فوراً بنفسك: أبطل توكن GitHub الحالي من إعدادات حسابك وأصدر واحداً جديداً محدود الصلاحية إن احتجت وكيلاً مستقبلاً، وكذلك توكن Vercel.** هذا القسم ليس جزءاً من عمل الوكيل — هو إجراء تنفّذه أنت.

كذلك، تقرير v4 يسرد 5 إجراءات **لا يمكن لأي وكيل تنفيذها نيابة عنك** (App Secret من Facebook، ربط صفحتك الحقيقية، توكن BotFather، مفاتيح الدفع/الدعم، وربط cron-job.org). **دون هذه الخطوات، لن يتحقق أي اختبار حي لتدفق البيانات الفعلي بغض النظر عن مدى إتقان الكود** — فرّق بين هذا وبين خلل برمجي عند أي فحص مستقبلي.

---

## 0.5 ملاحظة تسمية مهمة + ما فعله الوكيل من تلقاء نفسه بين الجولتين

أعدت فحص المستودع الآن (بعد أن أعددتُ مسودة هذه الخطة) ووجدت أن الوكيل نفّذ من تلقاء نفسه سلسلة commits حمل عليها تسمية **"v5" الخاصة به هو** (`§0` إلى `§9`) — وهي **مبادرة مختلفة تماماً عن هذه الخطة**، لا تتقاطع معها إلا جزئياً. لتفادي أي التباس، أعدت تسمية هذه الخطة إلى **v6**.

**ما نفّذه الوكيل فعلاً في مبادرته الخاصة (تحققتُ منه، وهو عمل هندسي جيد يُحسب له):**
- إعادة تنظيم المستودع: نقل كل ملفات `test_*.py` من جذر الحزمة إلى `tests/` مخصص
- تنظيف `ruff` بالكامل (1168 → 0 مخالفة) + إضافة CI workflow
- اختبارات "هرمتيك" لا تعتمد على ترتيب التشغيل (266 اختبار × 3 ترتيبات مختلفة)
- إصلاح مشاكل أداء N+1 في الاستعلامات مثبتة بـ`EXPLAIN` فعلي + فهارس جديدة
- فحص a11y عام اكتشف "3 عيوب بصرية حقيقية" وأصلحها
- نقطة فحص جاهزية (readiness probe) عميقة + ربط `request-id` للتتبع

**لكن — وهذا أهم شيء في هذا القسم — تحققتُ مباشرة أن أياً من مطالب خطتي الخمسة (i18n الأرقام، مراجعة الإتاحة الشاملة بالنسب المذكورة، Sentry/GlitchTip، خط أساس Lighthouse، تنبيه انقطاع الكرون) لم يُنفَّذ ضمن مبادرة الوكيل هذه:**

| ما فحصته الآن | النتيجة |
|---|---|
| هل أُصلح `StatsSection.tsx:22` (الرقم بلا locale)؟ | **لا — لا يزال `toLocaleString()` بلا معامل، كما هو** |
| هل يوجد ملف تنسيق مركزي؟ | **مفاجأة جزئية: ملف `src/lib/format.ts` موجود فعلاً الآن** — لكن **14 ملفاً آخر لا يزال يستدعي `toLocaleString(` مباشرة خارجه** — أي أن الملف أُنشئ لكن لم يُستخدم لتوحيد الاستدعاءات المتفرقة بعد. **مهمة الفريق 1 أدناه تُعدَّل لتصبح: أكمل الربط بدل الإنشاء من الصفر.** |
| هل تحسّنت نسبة `aria-label`/الأزرار؟ | **لا — 41/159 بالضبط كما كانت قبل مبادرة الوكيل، لم تتغير رغم "فحص a11y" الذي أجراه (فحص عام غير منهجي شامل، وليس الجرد الكامل المطلوب في المحور ب أدناه)** |
| هل أُضيف Sentry/GlitchTip؟ | **لا — لا وجود له في `package.json` ولا `requirements.txt`** |
| هل يوجد خط أساس Lighthouse موثَّق؟ | **لا — لا ملف `docs/*lighthouse*` من أي نوع** |
| هل يوجد كشف انقطاع نبض الكرون؟ | **لا — لا إشارة لـ`last_heartbeat`/كشف تأخر في أي راوتر** |

**الخلاصة:** كل محاور هذه الخطة (أ-هـ أدناه) **لا تزال قائمة بالكامل دون أي تغيير جوهري**، رغم أن الوكيل عمل بجد على جبهة مختلفة (جودة كود الباك-إند وتنظيم المستودع). هذا عمل جيد يستحق الإشادة، **لكنه لا يُغني عن تنفيذ الخطة التالية حرفياً.**

---

## 1. ما تحقق فعلاً بعد الفحص المباشر (لا يُعاد فتحه في هذه الخطة)

منقول من فحصي المباشر بعد v4 (ليس من رسالة commit فقط):
- `grep -c 'font-heading:' globals.css` = 1 (وليس 2) ✅
- `grep -rl "text-orange\|bg-orange\|border-orange"` = صفر ملف ✅
- `docs/v4-final-report.md` موجود بـ91 سطراً ويحتوي أدلة تيرمينال فعلية لكل بند (لا نثر) ✅

**ملاحظة جانبية مثيرة للاهتمام اكتشفها الوكيل ووثّقها بنفسه**: مشروع Smart-Menu المرجعي (الذي طلبتَ مطابقته) **يحتوي هو نفسه نفس عطل تعريف الخط المزدوج** الذي أصلحناه في SmartBot. أي أن SmartBot الآن، في هذه النقطة تحديداً، **أدق من المرجع نفسه**. هذا قرار لك: إن أردتَ إصلاح نفس السطرين في Smart-Menu لاحقاً، هو إصلاح بسيط (سطران) — لكنه خارج نطاق هذه الخطة لأنه مستودع منفصل.

---

## 2. الطبقة الجديدة: 5 محاور "عالمية المستوى" — كل واحد بأدلة أولية من فحصي + منهجية إلزامية

### المحور أ — دقة الأرقام والتواريخ (i18n) — عثرت على عدم اتساق فعلي

فحصت كل استخدامات `toLocaleString(` في المشروع (16 موضعاً). **15 منها** تمرر `"ar-LY"` أو `"ar-EG"` صراحة كمعامل locale. **موضع واحد فقط شذّ عن القاعدة**:

```
src/components/landing/sections/StatsSection.tsx:22
  return <span ref={ref} dir="ltr">{count.toLocaleString()}</span>
```

بلا معامل locale، هذا العداد المتحرك في صفحة الهبوط (على الأرجح عداد "عدد الصفحات المشتركة" أو مشابه) يعرض الرقم بتنسيق مختلف عن كل رقم آخر في التطبيق (قد يُظهر فواصل آلاف بأسلوب مختلف عن البقية، وقد يتغير حسب لغة متصفح الزائر بدل الثبات على العربية الليبية كبقية الموقع). **هذا أول مكان يراه أي زائر جديد على صفحة الهبوط** — وهو غير متسق فعلياً مع بقية الموقع.

**المهمة الإلزامية (فريق مخصص، وكيل واحد):**
1. **ملاحظة محدَّثة بعد الفحص الأخير:** ملف `src/lib/format.ts` **موجود بالفعل** الآن (أنشأه الوكيل ضمن مبادرته الخاصة) — **لا تنشئ ملفاً جديداً**، افتحه أولاً وتحقق من الدوال المعرَّفة فيه فعلياً، ثم استخدمها.
2. أصلح `StatsSection.tsx:22` ليستخدم دالة `format.ts` (أو، إن كانت الدالة المناسبة غير موجودة فيه بعد، أضِفها هناك) بدل `toLocaleString()` بلا معامل — يجب أن تطابق النمط المستخدم في كل مكان آخر (`"ar-LY"`).
3. نفّذ مسحاً شاملاً لبقية الـ14 ملفاً المتبقية (تحقق من العدد الفعلي وقت التنفيذ بـ`grep -rl "toLocaleString(" src --include="*.tsx" | grep -v "/lib/"`) التي لا تزال تستدعي `toLocaleString(`/`toLocaleDateString(` مباشرة بدل المرور عبر `format.ts` — وحّدها جميعاً عبر الدالة المركزية. تحقق أيضاً من اتساق صيغة عرض التاريخ (يوم/شهر/سنة؟ 12 أم 24 ساعة؟) عبر كل الملفات المُوحَّدة.
3. **بوابة تحقق:** لقطة شاشة من صفحة الهبوط الحية تُظهر الرقم بصيغة عربية-ليبية متسقة، بالإضافة لجدول يسرد كل موضع عرض تاريخ في التطبيق وصيغته بعد التوحيد.

### المحور ب — الإتاحة (Accessibility / WCAG) — فجوة كمية موثقة

فحصت نسبة `aria-label` إلى عدد أزرار `<button>`/`<Button>` في كامل `src/`: **159 زر** مقابل **41 استخدام فقط لـ`aria-label`** في كل الملف (وهذا الرقم يشمل استخدامات أخرى غير الأزرار أيضاً، فالنسبة الفعلية للأزرار المسمّاة أقل من ذلك). هذا يعني على الأرجح **عشرات الأزرار التي تحتوي أيقونة فقط بلا نص مرئي** (نمط شائع جداً في هذا المشروع، مثال: `<Button variant="ghost" size="sm"><ChevronLeft/></Button>`) لا تحمل أي تسمية يقرأها قارئ الشاشة — بمعنى أن مستخدماً يعتمد على قارئ شاشة لن يفهم وظيفة عشرات الأزرار في لوحة التحكم إطلاقاً.

**ملاحظة قبل البدء:** الوكيل يدّعي في مبادرته الخاصة إصلاح "3 عيوب بصرية حقيقية" ضمن فحص a11y عام — تحقق منها أولاً (ابحث عن ذكرها في تقرير commit `af6c2524`) لتفادي إعادة العمل عليها، لكن لا تعتبرها كافية: النسبة الكمية (41 تسمية مقابل 159 زر) **لم تتغيّر إطلاقاً**، أي أن الفحص كان موضعياً لا شاملاً كما هو مطلوب هنا.

**المهمة الإلزامية (فريق من 3 وكلاء، مقسّمين حسب مجلد `dashboard/` أبجدياً):**
1. جرد آلي: `grep -rn "<Button" src --include="*.tsx" -A2 | grep -B2 "^\s*<[A-Z][a-zA-Z]*\s*/\?>$"` (أو ما يعادله) لإيجاد كل زر لا يحتوي نصاً مباشراً بل أيقونة فقط.
2. لكل زر من هذه القائمة، أضف `aria-label` بوصف دقيق للوظيفة الفعلية بالعربية (وليس نصاً عاماً مكرراً مثل "زر").
3. تحقق من تباين الألوان (Color Contrast) بين النص والخلفية لكل توليفة نص/خلفية أساسية تستخدم توكن `--muted-foreground` على `--background` وعلى `--card` — استخدم أداة فحص تباين مفتوحة المصدر (`@adobe/leonardo-contrast-colors` أو ببساطة صيغة WCAG اليدوية عبر سكريبت) للتأكد من نسبة 4.5:1 على الأقل للنص العادي، خاصة في الوضع الفاتح حيث `--accent-fg` الجديد أُشير في تقرير v4 إلى أنه ضُبط عند 0.40 خصيصاً لتحقيق AA على الأبيض — تحقق أن هذا صحيح فعلياً بالقياس لا بالافتراض.
4. تأكد أن كل عنصر تفاعلي (رابط، زر، تبويب) قابل للوصول إليه وتفعيله بلوحة المفاتيح فقط (Tab + Enter/Space) بدون فأرة — اختبر يدوياً على 5 صفحات حرجة على الأقل (تسجيل الدخول، الاشتراك، لوحة البيانات الرئيسية، الوارد، الإعدادات).
5. **بوابة تحقق:** تقرير `docs/accessibility-audit-v6.md` يسرد كل زر تم إصلاحه + نتيجة فحص التباين لكل توليفة لون + فيديو/تسلسل لقطات لاختبار التنقل بلوحة المفاتيح.

### المحور ج — المراقبة والموثوقية التشغيلية (Observability) — فجوة كاملة، غير موجودة إطلاقاً

فحصت `package.json` و`requirements.txt`: **لا يوجد أي نظام تتبع أخطاء (Sentry أو مكافئ) في أي من الواجهة أو الباك-إند.** بمعنى: إن حدث خطأ لعميل حقيقي بعد الإطلاق (صفحة بيضاء، طلب API فاشل، استثناء غير متوقع)، **لن تعلم أنت بحدوثه أبداً إلا إذا اشتكى العميل مباشرة** — لا تنبيه، لا سجل مركزي، لا رؤية.

**المهمة الإلزامية (فريق من وكيلين):**
1. أضف [`GlitchTip`](https://glitchtip.com) (بديل مفتوح المصدر متوافق بالكامل مع SDK الخاص بـ Sentry، لا يتطلب اشتراكاً مدفوعاً إجبارياً) أو حساب Sentry مجاني (5,000 حدث/شهر مجاناً — يكفي مرحلة الإطلاق) — القرار الحاسم: **استخدم Sentry SDK القياسي مع endpoint قابل للتهيئة**، بحيث يمكن التحول لاحقاً لـGlitchTip ذاتي الاستضافة دون تغيير كود التطبيق، لأن الـSDK متوافق بينهما.
2. دمج الـSDK في: الواجهة الأمامية (`@sentry/nextjs`) — يلتقط أخطاء React غير المُعالجة + فشل الطلبات، والباك-إند (`sentry-sdk` لبايثون/FastAPI) — يلتقط استثناءات غير متوقعة في أي راوتر.
3. اربط تنبيهاً فورياً (بريد إلكتروني أو نفس بوت تليجرام الموجود مسبقاً في المشروع — استخدم `telegram_bot.py` الحالي بدل بناء قناة تنبيه جديدة من الصفر) عند حدوث أي خطأ من فئة "حرج" (500 من الباك-إند، استثناء React غير مُعالَج).
4. **بوابة تحقق:** تسبّب عمداً بخطأ اختباري تحكمي (مثال: استدعاء endpoint وهمي غير موجود) وتأكد أن التنبيه يصل فعلاً لتليجرام الأدمن خلال دقيقة، بدليل لقطة شاشة من محادثة تليجرام الحقيقية.

### المحور د — الأداء (Performance / Core Web Vitals) — لم يُقَس مطلقاً حتى الآن

كل الجولات السابقة (v1-v4) ركّزت على الصحة الوظيفية والبصرية، ولم تتضمن أي قياس أداء رقمي فعلي. "المستوى العالمي" يشمل سرعة التحميل بالضرورة.

**المهمة الإلزامية (فريق من وكيلين):**
1. شغّل Lighthouse (مدمج في Chrome DevTools، أو عبر `npx unlighthouse` مفتوح المصدر للمسح الشامل لكل الصفحات دفعة واحدة) على رابط الإنتاج الحي لأهم 8 صفحات (الهبوط، التسعير، تسجيل الدخول، الاشتراك، لوحة البيانات، الوارد، الإعدادات، الديمو). سجّل الدرجات الأربع (الأداء/الإتاحة/أفضل الممارسات/SEO) لكل صفحة في `docs/lighthouse-baseline-v6.md`.
2. لكل صفحة تقل درجة الأداء فيها عن 90: افحص السبب الفعلي من تقرير Lighthouse نفسه (لا تخمين) — الأسباب الشائعة المتوقعة بناءً على فحصي الأولي: 6 ملفات لا تزال تستخدم `<img>` خام بدل `next/image` (تحقق من كل واحد وحوّله ما لم يكن هناك سبب تقني وجيه للاستثناء)، حجم حزمة JavaScript الكلي (شغّل `next build` وافحص تقرير الحجم لكل صفحة، وأي مكتبة ضخمة مستوردة بالكامل بدل استيراد جزئي — خاصة `lucide-react` إن كان يُستورد كحزمة كاملة بدل أيقونات منفردة، و`framer-motion`).
3. أصلح كل سبب مكتشف تحديداً (لا "تحسينات عامة للأداء") ثم أعد قياس Lighthouse على نفس الصفحة للتأكد من تحسن رقمي فعلي موثَّق (قبل/بعد).
4. **بوابة تحقق:** جدول `docs/lighthouse-baseline-v6.md` بعمودين (قبل v6 / بعد v6) لكل صفحة من الثمانية، مع هدف صريح: لا صفحة تقل درجة أداء فيها عن 85.

### المحور هـ — موثوقية جدولة الكرون (خطر معماري صريح اعترف به الوكيل نفسه)

تقرير v4 يعترف صراحة: خطة Vercel Hobby الحالية لا تدعم كرون أكثر من مرة يومياً، فالحل الحالي يعتمد على **خدمة خارجية مجانية طرف-ثالث** (`cron-job.org`) لتشغيل النبض كل 5 دقائق. هذا يعني أن جزءاً جوهرياً من موثوقية المنشورات المجدولة والدورات الخلفية **يعتمد الآن على توفر خدمة مجانية خارجة عن سيطرتك الكاملة**.

**قرار حاسم واحد (لا خيارات متعددة):** بما أن هذا مشروع تجاري يُراد إطلاقه عالمياً بمعايير كاملة، أوصي بالترقية لخطة Vercel Pro (تدعم كرون بتردد أعلى من داخل نظام Vercel نفسه، يُزيل الاعتماد الكامل على طرف ثالث مجاني لخدمة جوهرية). إن قررتَ عدم الترقية حالياً لأسباب تكلفة: **يُلزَم الوكيل بإضافة نظام كشف صريح** — إن لم يصل أي نبض من `cron-job.org` خلال 15 دقيقة (تحقق من آخر `heartbeat` مسجَّل في قاعدة البيانات مقابل الوقت الحالي عبر فحص دوري من نقطة أخرى)، يُرسَل تنبيه فوري لتليجرام الأدمن — بحيث لا تكتشف توقف الجدولة إلا بعد شكوى عميل.

---

## 3. تعبئة الوكلاء لهذه الجولة — 5 فرق، ملفات/نطاقات منفصلة تماماً

| الفريق | المحور | عدد الوكلاء | الملفات/النطاق |
|---|---|---|---|
| 1 | i18n الأرقام والتواريخ | 1 | `StatsSection.tsx` + كل ملف يعرض تاريخاً/رقماً + `src/lib/format*.ts` جديد |
| 2 | الإتاحة | 3 (مقسّمين حسب مجلدات `dashboard/[a-h]`, `[i-p]`, `[q-z]+الصفحات العامة` أبجدياً) | كل زر/رابط في المشروع |
| 3 | المراقبة (Sentry/GlitchTip) | 2 | إعداد SDK فقط، لا تعديل منطق أعمال |
| 4 | الأداء | 2 | تحليل + إصلاحات محددة بالصور/الحزم |
| 5 | موثوقية الكرون | 1 | نظام كشف انقطاع النبض + تنبيه تليجرام |

كل فريق يعمل بالتوازي بلا تعارض (نطاقات منفصلة تماماً). التنسيق عبر `docs/v6-coordination-log.md` كما في v4.

---

## 4. جدول القبول النهائي — v6

| # | البند | معيار PASS |
|---|---|---|
| 1 | كل عرض رقم/تاريخ في المشروع يستخدم دالة تنسيق مركزية موحّدة | صفر استدعاء مباشر متفرق لـ`toLocaleString`/`toLocaleDateString` خارج `src/lib/format*.ts` |
| 2 | كل زر أيقونة-فقط يحمل `aria-label` وصفياً دقيقاً | تقرير `docs/accessibility-audit-v6.md` كامل |
| 3 | نسبة تباين AA (4.5:1) محققة لكل توليفة نص/خلفية أساسية | قياس فعلي موثَّق لكل توليفة |
| 4 | نظام تتبع أخطاء حي يرسل تنبيهاً فعلياً عبر تليجرام | اختبار خطأ متعمد ينتج تنبيهاً حقيقياً موثقاً بلقطة شاشة |
| 5 | درجة أداء Lighthouse ≥ 85 لكل صفحة من الثمانية الأساسية | جدول قبل/بعد كامل |
| 6 | تنبيه تلقائي عند انقطاع نبض الكرون لأكثر من 15 دقيقة | اختبار متعمد (إيقاف مؤقت) يُنتج تنبيهاً فعلياً |
| 7 | قرار صريح موثَّق حول ترقية خطة Vercel (نُفِّذ أو رُفض بسبب مذكور) | سطر واحد في `docs/v6-final-report.md` يوضح القرار والسبب |

**لا توقف قبل اكتمال الجدول أعلاه بالكامل بدليل فعلي — وبعده مباشرة، أنت من ينفّذ القسم 0 (إبطال التوكنات) بنفسك.**
