# SmartBot — خطة الجولة v10 (الأضخم والأعمق): gstack + 11 وكيل تشخيص + تنفيذ موجي

**التاريخ:** 2026-09-06 · **الأساس:** main @ 38537f9e (v9 مكتملة ومنشورة) · **الوInvoker:** المالك (طلب مباشر)

## 0) تشخيص ما قبل الخطة (11 وكيلًا بتسليح gstack)

ثُبّتت gstack (garrytan/gstack) كاملة (55 مهارة) واستُخدمت منهجياتها الفعلية عبر 11 وكيلًا متوازيًا:

| الوكيل | أداة gstack | أبرز النتائج |
|---|---|---|
| G1 | /cso --comprehensive | 17 نتيجة: CRIT (أسرار Neon+SECRET_KEY في تاريخ git العام d7e5d8db) · 3 HIGH · 7 MED |
| G2 | /qa-only | 9 أعطال (4×P2): ads 400 مزدوج · cron/status 403 · تحقق إنجليزي · هبوط دخول خاطئ — صحة عامة 88/100 |
| G3 | /benchmark | خطوط تُعاد كل تنقل (no-store) · stats يُطلب 3× · LCP /pricing 2563ms (حركة) · JS FAIL 14/14 |
| G4 | /design-review | 4×P2 تباين (مصادقة 2.6:1، حدود داكنة 1.2:1) + 10×P3 — RTL 32/32 سليم، الإنتاج=المحلي |
| G5 | /devex-review | TTHW الموثق معطوب (أمر README ينهار) · .env.example ينخر نفسه · DX 3.5/10 |
| G6 | /health | درجة 7.6/10 · غطاء 50% · 7 وحدات 0% · 155 ruff حقيقي (7 ASYNC حاجبة) · صادرات واجهة ميتة 39.5% |
| G7 | /investigate (Sentry) | توكن Sentry أُبطل (يدويًا من المالك) — عبر مسار بديل: 3 أعطال إنتاج حقيقية (SPA نطاق API هامدة منذ v6، ترحيلات Alembic لا تعمل، نداء Graph بتوكن فارغ) |
| S1 | specialists/testing | backlog ~110 اختبارات · شبكة انحدار 401 شبه غائبة (233 نقطة حماية) |
| S2 | specialists/api-contract | 8 انحرافات — CRITICAL: wallet-cap ثلاثي الاتجاه (خطر مالي) |
| S3 | specialists/maintainability | 30 نتيجة · inbox_engine ميت 390 سطرًا واختباراته تختبر النسخة الميتة · 4 استيرادات دائرية لـrunner |
| S4 | specialists/simplification | ~3,324 سطرًا قابلًا للإزالة · بناء Vite قديم 5.5MB يُنشر على نطاق API · تبعيات ميتة |

**التقاطعات المؤكدة بين الوكلاء (تقاطع أدلة مستقل):** نداء Graph الفارغ (G2+G7) · حزمة JS (G3+G6) · الكود الميت (S3+S4+G6) · هبوط /admin (G2+G1-منطق الأدوار).

---

## المسار A — أمن الباكند (من G1) [Wave 1 — وكيل W1a]

| # | الإصلاح | الموضع | التفصيل |
|---|---|---|---|
| A1 | **تقييد بحث المصادقة بالمستأجر** | auth.py:53 (`get_current_user`) + auth.py:113 (login) + runner.py:917 (WS) | الجلسة تحمل tenant_id (jti موجود) — استخدمه: `where(User.username==sub, User.tenant_id==tid)`؛ للـlogin العام اتبع نمط v8: username أولًا ثم email مع تحييد MultipleResultsFound نهائيًا. يغلق HIGH#3 (إقفال متبادل بين مستأجرين) |
| A2 | **بوابة صلاحيات أدوات الوكيل** | agent_engine.py:192-198 (+49) | أفعال إدارية (toggle_bot/إدارة قواعد المنصة) تتطلب role="admin" لمستأجر الوكيل؛ الوكيل يعمل بمفتاح المنصة (`_fb_client`) — قيّد الأدوات الإدارية للأدمن فقط. يغلق HIGH#4 (editor يوقف بوت المنصة) |
| A3 | **allowlist لمسار /api/support/info العام** | support.py:50-56 | لا يُرجِع SystemConfig كله — allowlist صريحة (البريد/الهاتف/واتساب/ساعات العمل فقط). يغلق MED#7 (تسريب نبضات cron وtelegram_chat_id) |
| A4 | **list_stats حسب المستأجر** | agent_engine.py:226-227 | أضف `where(Reply.tenant_id==ctx.tenant_id)` (نفس نمط v4 §3.6) |
| A5 | **create_rule بمعرف المستأجر** | agent_engine.py:217 | `tenant_id=ctx.tenant_id` (التنويعة الصحيحة موجودة في onboarding.py:208) |
| A6 | **قناع أسرار التكامل في GET admin** | admin_routes.py:176 (+105) | أعد قيمًا مقنّعة (****آخر4) — لا خامًا. يغلق MED#11 |
| A7 | **تعقيم أسماء المعلقين في PDF** | pdf_reports_engine.py:392 (+186) | escape HTML entities قبل بناء جدول PDF (حقن HTML وسيط weasyprint) |
| A8 | **حراسة SSRF لأداة image_analyze** | ai_service.py:304 | https فقط + رفض المضيفات الخاصة (127/10.x/192.168/169.254/localhost) قبل الجلب |
| A9 | **عدم تسريب str(e) من محرك الوكيل** | agent_engine.py:163 | رسالة عامة عربية + التسجيل الداخلي فقط |
| A10 | تقييد نسب نجاح/فشل الدفع بصاحب الصفحة | ai.py (إن لزم) | ضمن نطاق A2 |

**إجراءات المالك (لا تُنفَّذ برمجيًا هنا — تُذكر في التقرير النهائي بإلحاح):**
1. **تبديل كلمة مرور Neon فورًا** (نافذة تعرض منذ 2026-07-06، الريبو عام).
2. **تدوير SECRET_KEY + FERNET_KEY** في بيئة الإنتاج (يبطل الجلسات — مقبول).
3. تدوير كلمة admin ثم scrub تاريخ git (git filter-repo) + force-push (بعد موافقة المالك).
4. توكن Sentry جديد بنطاقات قراءة (اختياري للرصد).

## المسار B — أعطال الإنتاج الحقيقية (من G7+G2) [Wave 1 — W1b خلفي + W2 أمامي]

| # | الإصلاح | الموضع | التفصيل |
|---|---|---|---|
| B1 | **تصحيح مستوى static/ في مزامنة nطاق API** | scripts/sync_next_static.py:68-69 | `dst = STATIC/"_next"/"static"` — ثم إعادة مزامنة بعد البناء (المنسِّق) — يحيي SPA api.smart-link.ly الهامدة منذ v6 ويحدّث خطوط الخادم المتقادمة (S4) |
| B2 | **إصلاح ترحيلات Alembic** | runner.py:303 | `fromlist=["upgrade"]` (المجاور 297 سليم) — يوقف ابتلاع الخطأ الصامت |
| B3 | **حراسة نداء Graph بتوكن فارغ** | dashboard_stats.py:52,202 (+_services.py:25) | لا تُنشئ FBClient ولا تستدعِ Graph إذا كان التوكن/الصفحة فارغين → قيم null بدل 400 لكل تحميل (يغلق G2-01 ads أيضًا: الرد 200 بلا حسابات بدل 400) |
| B4 | **بطاقة cron للأدمن المنصة فقط** | الواجهة: صفحة /admin | لا تطلب /api/cron/status إلا للأدمن المنصة (tenant_id===0) — يغلق 403 (G2-02) |
| B5 | **هبوط دخول مالك النشاط → /dashboard** | منطق إعادة التوجيه بعد login | الأدمن المستأجر (tenant_id≠0) → /dashboard؛ المنصة فقط → /admin (G2-04) |
| B6 | **تعريب رسائل تحقق النماذج** | LoginForm/RegisterForm | novalidate + تحقق JS عربي (فارغ/بريد/طول) — توحيد التجربة (G2-03) |
| B7 | **P3 تفاعلية**: Escape يغلق النماذج الموسّعة (autoreply/broadcast/تغيير كلمة المرور) · التركيز لأول حقل عند الفتح · إزالة ازدواج رسالة الدخول الخاطئ | مواضع G2-05/06/07 | حُسم: النماذج الموسّعة inline ليست حوارات (role=dialog غير صحيح دلاليًا — axe سليم) — يُوثّق القرار |

## المسار C — الأداء (من G3) [Wave 1 — W3]

| # | الإصلاح | الموضع | الأثر المقاس |
|---|---|---|---|
| C1 | **استثناء الخطوط من no-store** | middleware.ts:22 | woff2/woff → cache-control طويل (public, max-age=31536000, immutable للـhashed) — يوفر ~210KB لكل تنقل |
| C2 | **إلغاء تثليث طلبات /api/public/stats** | usePublicStats.ts | وعد مشترك على مستوى الوحدة (التعليق يقول single fetch والكود لا يفعل) |
| C3 | **أول كلمة kinetic فورية** | مكون النص الحركي /pricing | الكلمة الأولى تُعرض بلا انتظار الحركة (LCP) — إصلاح فشل LCP الإنتاجي 2563ms |
| C4 | **تأجيل recharts عن التبويب الافتراضي /demo** | صفحة demo | lazy للرسم في التبويب غير الافتراضي (−352KB من أول تحميل لأثقل صفحة عامة 987KB) |

## المسار D — عقد API (من S2) [Wave 1 — W1a خلفي + W2 أمامي]

| # | الإصلاح | الموضع |
|---|---|---|
| D1 | **توحيد wallet-cap**: الفرض في payments.py يقرأ قيمة DB (من admin config) + PaymentDialog يقرأها من /api/config بدل `>99` الصلب | payments.py:121 + PaymentDialog.tsx:56 |
| D2 | **total داخل غلاف الدعم** | support.py (tickets) |
| D3 | **404/405 بمخطط {detail} JSON** لغير المعرّف عبر spa_catch_all | runner.py:1199 |
| D4 | **توسيع ApiUser بحقول /api/me الفعلية** | types.ts |

## المسار E — تطهير الكود الميت (من S4+G6+S3) [Wave 2 — W4 — بعد Wave 1]

| # | الحذف | الحجم | إثبات |
|---|---|---|---|
| E1 | fb_dashboard/migration.py | 81 سطرًا | صفر مستورد (S4+G6) |
| E2 | fb_dashboard/inbox_engine.py + اختبارات النسخة الميتة | 390+ | الراوتر يعيد التنفيذ inline؛ الاختبارات الحالية تختبر النسخة الميتة (ثقة زائفة — S3) |
| E3 | fb_dashboard/subscription_decisions.py | 41 | منطقه حي مكرر ومشدّد ذرّيًا في runner+payments |
| E4 | 17 تابعًا ميتًا عبر المحركات (get_activity_log، get_conversation_stats، Timer، get_handler، get_tool…) | 265 | جرد S4 |
| E5 | صادرات الواجهة الميتة (motion.ts ~25، Motion* ~30، apiJson/unwrapBody/KineticHeading) | 85/215 | جرد G6 |
| E6 | تبعيات ميتة: sse-starlette، react-is، autoprefixer (بعد تحقق grep) | — | S4 |
| E7 | بناء Vite القديم من جذر النشر (95 ملفًا/5.5MB يُنشر على نطاق API بلا مرجع HTML حي واحد) | 5.5MB | S4 (الأصول ترد 200 إنتاجيًا) |
| E8 | 13 توصية noqa ميتة في _observability.py | — | G6 |
| E9 | manual_tests/ (6 ملفات) | 1,114 | G6+S4 |
| E10 | تثبيت actions بـ SHA + CODEOWNERS + عدم النشر عند فشل CI | .github/workflows/ci.yml | G1#13 |

**يُحسم بالإبقاء (قرار توثيق):** منظومة الوكيل/AI كاملة (954 سطرًا) رغم صفر مستهلك واجهة — ميزة منتج قادمة، تُقفل ثغرة تصعيدها بـ A2.

## المسار F — صحة الكود (من G6) [Wave 1 — W1b]

- F1: إصلاح 7 استدعاءات ASYNC حاجبة (ai_service.py:257، bot.py:1157/1159، runner.py:177/181/296 → to_thread).
- F2: إصلاح 10 Optional ضمنية + 3 قيم افتراضية mutable (ruff).
- F3: (المنسِّق) عتبة `--cov-fail-under=50` في بوابة الجودة.

## المسار G — شبكة الانحدار (من S1 + إصلاحات v10) [Wave 2 — W5]

- G-suite: `tests/test_v10_security.py` — انحدارات كل إصلاحات A/D/B (تكرار اسم عبر مستأجرين لا يكسر الدخول · editor لا يوقف بوت المنصة · support/info لا يسكب SystemConfig · list_statsscoped · create_rule بtenant · wallet-cap من config · أسرار مقنعة · 404/405 JSON).
- شبكة 401: مسح سالب لأهم الراوترات (نمط probe الأخضر من S1 — 30+ اختبارًا محوريًا).
- conftest.py بحزام fixtures مشترك (إضافي — لا يكسر الموجود).
- هدف: 307 → 360+ اختبارًا · الغطاء 50% → 56%+.

## المسار H — تجربة المطوّر (من G5) [Wave 3 — المنسِّق]

- H1: تصحيح أمر التشغيل في README (الصحيح: `python -m uvicorn runner:app --app-dir fb_dashboard --port 8000`).
- H2: إصلاح .env.example (إزالة التعليقات المضمّنة كقيم · DEBUG=true نمط التطوير مع شرح).
- H3: توثيق LOCAL_API_PROXY للواجهة المحلية.
- H4: تصحيح مسارات أوامر البوابات من الجذر.

## المسار I — إصلاحات بصرية (من G4) [Wave 1 — W3]

- I1: صفحات المصادقة: `muted-foreground/60 → /80` (زر العودة + سطر الحقوق 2.6:1 → AA).
- I2: رابط التبديل accent/80: تسطير عند hover (3.74:1 + إشارة بصرية).
- I3: توكن `--input` الداكن: رفع حدود الحقول (1.4.11).
- (P3 المختار): بطاقة التسعير اليتيمة الرابعة → إخفاء عند الشبكة الفردية (موفّر: 4→3) — إن كان رخيصًا.

## المُؤجَّل صراحةً (أمانة كاملة)

| البند | السبب |
|---|---|
| تفكيك runner.py/bot.py/PaymentDialog البنيوي | M-L جهد — جولة مستقلة (خريطة S3 جاهزة) |
| توحيد الترقيم الأربعة + فرض ok() في 9 راوترات | يتطلب لمس 30+ نقطة — جولة مستقلة |
| سقف تكلفة LLM لكل مستأجر | قرار منتج (تسعير) — G1#10 TENTATIVE |
| حماية replay للويبهوكات | G1#14 LOW TENTATIVE (توقيع HMAC قائم) |
| اختبارات وحدة الواجهة (vitest) | بنية تحتية جديدة — جولة مستقلة |
| سلب G2-09 (حقول business_name/phone في التسجيل) | قرار منتج (احتكاك التسجيل مقابل اكتمال البيانات) |
| ميزانية JS الشاملة (<500KB) | يتطلب تقسيم حزم كبير — يُوثّق الاتجاه (900KB للوحة بعد −31% من v9) |
| scrub تاريخ git | إجراء مالك (يؤثر على كل المستنسخين) |

## معايير القبول (إلزامية — أدلة، لا تصريحات)

| # | المعيار | الدليل المطلوب |
|---|---|---|
| 1 | كل إصلاحات A/B/C/D/E/F/G/H/I مُطبقة | gate_all.sh ALL GREEN + pytest 360+ |
| 2 | صفر انحدار أمني | test_v10_security.py أخضر + مسح 401 |
| 3 | SPA نطاق API حي | بعد الدفع: /_next/static/chunks/*.js يرد 200 على api.smart-link.ly |
| 4 | الأداء محسّن | خطوط بكاش (curl ترويسة) + stats مرة واحدة + recharts خارج أول تحميل /demo (شبكة) |
| 5 | الكود الميت مطهّر | grep استيرادات المحذوفات = 0 · npm deps أقل 3 · ~3,000+ سطرًا أقل |
| 6 | DX | نسخة نظيفة تتبع README تعمل (أمر واحد صحيح) |
| 7 | التباين AA صفحات المصادقة | قياس نسب ≥4.5:1 |
| 8 | commit واحد batch → push main+develop → تحقق إنتاج حي | إثبات الإنتاج بعد النشر |

**الالتزام الثابت: لا توقف قبل إثبات حي من الإنتاج.**
