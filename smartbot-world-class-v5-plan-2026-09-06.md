# SmartBot v5 — خطة الإتقان العالمي (World-Class Mastery Plan)

> **التاريخ:** 2026-09-06 | **المؤلف:** الوكيل — بناءً على أمر المالك المباشر
> **السلسلة:** هذه هي الجولة الخامسة، بعد: parity-v2 → world-class-v3 → radical-v4. كل ما سبق منفّذ وموثّق بأدلة حية.
> **شروط التنفيذ (نفس شروط v4 وأقوى):** لا أسئلة — تنفيذ كامل حتى النهاية | كل بوابة يجب أن تكون خضراء 100% | كل ادعاء بدليل قابل للتكرار | دفع إلى main + develop | تحقق حي بالمتصفح على الإنتاج بعد الدفع | تقرير نهائي بجدول PASS | ملاحظات هندسية صادقة بلا تجميل.

---

## التشخيص المؤسِّس (لماذا v5؟ — أدلة من فحص مباشر اليوم)

| # | الاكتشاف | الدليل المباشر | الخطورة |
|---|---|---|---|
| D1 | **6 اختبارات تفشل في المجموعة الكاملة** وتنجح منفردة (`no such table: users`) | `pytest -q` → `6 failed, 260 passed`؛ تشغيل الملف وحده → نجاح كامل | 🔴 حرجة — البوابة ليست موثوقة |
| D2 | **29 ملف اختبار داخل جذر الحزمة** `fb_dashboard/test_*.py` بجانب كود الإنتاج | `ls fb_dashboard/test_*.py \| wc -l` → 29 | 🟠 تنظيمي — بنية غير قياسية |
| D3 | **لا يوجد أي linter** للمشروع (ruff غير مثبت، لا تكوين) | `pip list \| grep ruff` → فارغ | 🟠 جودة — بلا بوابة ثابتة |
| D4 | **لا CI workflow** — البوابات تعمل يدوياً فقط أثناء جلسات الوكيل | `.github/` يحوي branch-protection فقط | 🟠 موثوقية — الانحدار (D1) لم يكتشفه أحد |
| D5 | **15 ملف .md متناثرة في جذر docs/** بلا تصنيف (تقارير/خطط/لقطات مختلطة) | `ls docs/*.md \| wc -l` → 15 | 🟡 تنظيمي |
| D6 | **لا قياس تغطية اختبار** — لا نعرف أي مسار حرج غير مغطى | لا pytest-cov في المتطلبات | 🟠 مخاطرة تغطية |
| D7 | لا سكريبت `typecheck` مستقل في الواجهة (الفحص يحدث ضمن build فقط) | `grep package.json` → build/lint فقط | 🟡 أداة |

**الخلاصة:** v3/v4 أصلحا المنتج (المظهر والوظائف والبيانات). v5 تبني **المنظومة الهندسية** التي تُبقيه عالمياً: اختبارات محكمّة، هيكل قياسي، بوابات آلية، تغطية مقاسة، أداء مفحوص، وثائق احترافية — ثم تُثبت كل ذلك حياً على الإنتاج.

---

## §0 — إصلاح الانحدار الحرج: مجموعة اختبارات محكمّة (Hermetic)

**الهدف:** `pytest -q` كاملة خضراء 100% بأي ترتيب تشغيل، إلى الأبد.

1. عزل مصدر التلوث (اختبار يُسقط الجداول أو يستبدل المحرك/الاتصال في منتصف الجلسة فيفقد الجلسةُ التالية الجداول).
2. الإصلاح البنيوي: التأكد من أن كل اختبار يرى قاعدة بها مخطط كامل — بغض النظر عن من سبقه.
3. اختبار إثبات: تشغيل المجموعة كاملة مرتين متتاليتين + تشغيل معكوس (`--lf` إلغاء + ترتيب عكسي) — كلها خضراء.
4. بوابة دائمة: `scripts/gate_pytest.sh` تفشل إن فشل أي اختبار واحد.

**دليل القبول:** `pytest -q` → `0 failed` + تشغيل عكسي `0 failed` + تشغيل مزدوج متتالٍ `0 failed`.

## §1 — تنظيم المستودع بمعايير الصناعة

1. نقل الـ 29 ملف `fb_dashboard/test_*.py` → `tests/` مع conftest موحّد في الجذر يضمن الحزم والاستيراد.
2. تنظيف `fb_dashboard/` من أي بقايا اختبار (مجلد manual_tests يُدمج أو يُوثّق).
3. docs/: نقل الملفات المتناثرة إلى `docs/reports/` + `docs/plans/` + `docs/audit/` (بلا كسر أي روابط داخلية — مسح الروابط قبل/بعد).
4. مسح الكود الميت: وحدات غير مستوردة، دوال يتيمة، ملفات بلا مرجع — تُحذف أو تُوثّق كـ «سائق يدوي».
5. `pytest.ini` يُحدَّث للمسار الجديد + تأكيد أن Vercel لا يرفع مجلد tests (`.vercelignore`).

**دليل القبول:** صفر ملفات `test_*.py` داخل `fb_dashboard/` + المجموعة كاملة خضراء بعد النقل + بنية docs مصنّفة.

## §2 — بوابات جودة صلبة (ruff + typecheck + CI)

1. تثبيت `ruff` + ملف `ruff.toml` (خط 100، قواعد E/F/W/I/B/UP آمنة).
2. `ruff check fb_dashboard api tests` → صفر خطأ (إصلاح تلقائي + يدوي للمتبقي).
3. سكريبت `typecheck` صريح في frontend (`tsc --noEmit`) + بوابة موحّدة `scripts/gate_all.sh` (pytest + ruff + tsc + build).
4. **CI حقيقي**: `.github/workflows/ci.yml` يشغّل كل البوابات على كل push و PR إلى main — حتى لا يتكرر انحدار D1 أبداً بدون اكتشاف.
5. تحديث `requirements.txt` و `dev` dependencies بشكل صادق.

**دليل القبول:** `ruff check` → 0 + `tsc --noEmit` → 0 + ملف CI موجود وصالح البنية (YAML parse) + بوابة موحّدة تعمل محلياً.

## §3 — عمق الاختبار وقياس التغطية

1. تثبيت `pytest-cov` + تقرير تغطية للمحركات الحرجة: `bot.py`, `messenger_service.py`, `inbox_engine.py`, `routers/webhook*`, `analytics_engine.py`, `subscription_decisions.py`.
2. سرد الفجوات: كل فرع حرج غير مغطى (مسارات خطأ، مصادقة، عزل مستأجرين) يُضاف له اختبار مركّز.
3. حد أدنى مُعلن: تغطية سطور المحركات الحرجة ≥ 80% أو توثيق صادق لأي مسار متعذّر (اختبار تكامل خارجي حقيقي).
4. اختبارات صلابة جديدة: تشغيل مزدوج، ترتيب عكسي، عزل بيانات بين الاختبارات المتجاورة.

**دليل القبول:** تقرير تغطية رقمي + ≥ 12 اختباراً جديداً يرفعون المجموعة فوق 280 ناجحاً + بقاء كل الخضراء خضراء.

## §4 — الأداء والمتانة

1. تدقيق N+1 في النقاط الساخنة (`/api/inbox`, `/api/subscribers`, `/api/analytics/*`, `/api/comments`) — كل استعلام داخل حلقة يُجمع (selectinload / استعلام مجمّع).
2. فهارس DB ناقصة للمفاتيح الساخنة (المستأجر + الوقت) عبر ترحيل جديد مع إثبات `EXPLAIN` محلي أن الفهرس يُستخدم.
3. تحليل حزمة الواجهة (`next build` output): أي صفحة > 250KB JS تُفحص — تقسيم ديناميكي للمكونات الثقيلة.
4. رؤوس تخزين مؤقت للأصول الثابتة (favicon/manifest/OG) في Vercel.

**دليل القبول:** تقرير أداء بأسطر قبل/بعد (استعلامات + KB) بلا أي تراجع وظيفي.

## §5 — صقل الواجهة العالمي (جمال بلا فوضى)

1. Loading skeletons موحّدة لكل صفحات البيانات (inbox/audience/analytics/comments/campaigns).
2. Error Boundary لكل قسم مسارات (app/) — خطأ جسد لا يكسر التطبيق كله.
3. مسح حالات: فارغة (EmptyState)، تحميل، خطأ، بلا بيانات — لكل جدول/بطاقة/رسم.
4. صقل micro-interactions مقابل قاموس Smart-Menu في `lib/motion.ts` — توحيد الحركات المتنافرة.
5. فحص RTL كامل بعد كل التعديلات (375/768/1440 بلا overflow — إعادة تشغيل المسح الآلي).

**دليل القبول:** مسح الحالات يولّد تقريراً + مسح viewport 21/21 يبقى أخضر + لا layout shift بصري.

## §6 — الوصولية (a11y)

1. مسح لوحة المفاتيح: ترتيب Tab منطقي، لا فخوص تركيز، Escape يغلق كل ما يفتح بنقرة.
2. ARIA: كل زر أيقوني له تسمية، كل dialog role صحيح، كل حالة تحميل معلنة.
3. تباين الألوان: تدقيق الأزمنة الفاتح والداكن (AA 4.5:1 لنص الجسم) — توثيق أي استثناء.

**دليل القبول:** تقرير a11y بنقاط قبل/بعد + مسح آلي axe-core (إن توفر) أو مسح يدوي موثّق لكل صفحة.

## §7 — المراقبة والتشغيل

1. Middleware تسجيل طلبات منظّم (method, path, status, ms, tenant) — دون تسريب أسرار.
2. `/api/health` أعمق: DB + جدول محوري + زمن الاستجابة، مع صمام عدم فتح ثقوب (بلا تفاصيل داخلية للعامة).
3. تسريع الدورة الخلفية مراجعةً (heartbeat) — بلا تغيير معمارية Hobby.
4. بوابة rate-limit مراجعة على نقاط الدخول الحساسة (login, webhook, cron).

**دليل القبول:** `/api/health` حي يعرض الفحوص الجديدة + سجلات منظّمة موثّقة بمثال.

## §8 — توثيق بمستوى عالمي

1. `README.md` إعادة كتابة: ما هي المنصة، معمارية سطرية، تشغيل محلي بخطوات، متغيرات البيئة، البوابات، النشر — إنجليزية + قسم عربي.
2. `docs/ARCHITECTURE.md` جديد: خريطة الوحدات + تدفق الويبهوك + عزل المستأجرين + القرارات المعمارية الكبرى وسببها.
3. تحديث `docs/user-guide-ar.md` بأي تغييرات واجهة من v5.
4. فهرس `docs/INDEX.md` يربط كل شيء بعد إعادة التنظيم.

**دليل القبول:** README يعكس الحقيقة الحالية (متغيرات/أوامر مطابقة) + ARCHITECTURE بلا ادعاءات عامة — كل رسم نصي مطابق للكود.

## §9 — البوابات النهائية + الدفع + التحقق الحي + التقرير

1. `scripts/gate_all.sh`: pytest كاملة + ruff + tsc + next build — كلها خضراء في تشغيل واحد.
2. دفع إلى `main` + `develop` (نفس أمر الدفع المعتمد).
3. انتظار نشر Vercel (الواجهة + الـ API) — تحقق `healthz` + بصمة البناء الجديدة.
4. **تحقق حي بالمتصفح**: دخول المالك → كل صفحات لوحة التحكم → لا أخطاء كونسول → skeletons تظهر وتختفي → مسح viewport → E2E رحلة العميل v4 يبقى 8/8.
5. كتابة `docs/v5-final-report.md` بجدول PASS لكل قسم أعلاه بأدلة + ملاحظات هندسية صادقة + ما ينتظر المالك.

**دليل القبول (الحسم):** جدول §9 كامل PASS بأدلة من الإنتاج الحي + `git log` يظهر التزامات v5 على main.

---

## ترتيب التنفيذ الصارم

§0 (وقف نزيف الاختبارات) → §1 (تنظيم) → §2 (بوابات) → §3 (تغطية) → §4 (أداء) → §5 (واجهة) → §6 (a11y) → §7 (مراقبة) → §8 (توثيق) → §9 (دفع + تحقق + تقرير).

كل قسم يُغلق ببوابته قبل الانتقال للتالي. أي فشل = إصلاح فوري ثم إعادة البوابة — لا تجاوز ولا تسويف.
